# VoQuiz 코드 해설집
### — 함수 하나하나까지, 12살도 이해하는 완전 정복본 (대학 입시 면접용) —

> **이 책의 목표**
> 이 문서 하나만 읽으면 "이 함수는 어느 파일 몇 번째 줄에 있고, 무슨 일을 하고, 전체에서 왜 필요한지"를 입학 사정관에게 막힘없이 설명할 수 있습니다.
> 어려운 용어는 전부 **쉬운 비유**로 풀었고, 실제 코드 위치(파일명·줄번호)를 함께 적었습니다.
> HTML/CSS 디자인은 제외했고, **자바스크립트 동작**(랜딩 스크롤 애니메이션·다크모드·온디바이스 AI)은 포함했습니다.

---

## 목차

- **[머리말: 이 책을 읽는 법](#머리말-이-책을-읽는-법)**
- **[0장. 준비운동 — 꼭 알아야 할 개념 20가지](#0장-준비운동)**
- **[1장. 전체 구동 방식 — 가장 중요한 큰 그림](#1장-전체-구동-방식)**
  - 1-1. 프로그램은 세 덩어리로 나뉜다
  - 1-2. 버튼 한 번 누르면 벌어지는 일 (요청의 일생)
  - 1-3. 데이터의 대장정: 가입부터 리포트까지
  - 1-4. 두 개의 AI (온디바이스 vs 서버)
  - 1-5. 진동벨 구조 (백그라운드 + 폴링)
- **[2장. app.py — 서버의 심장](#2장-apppy)**
- **[3장. auth.py — 로그인과 보안](#3장-authpy)**
- **[4장. database.py — 데이터 창고](#4장-databasepy)**
- **[5장. progress.py — 진행바 엔진](#5장-progresspy)**
- **[6장. ai_extract.py — 단어 추출 AI](#6장-ai_extractpy)**
- **[7장. ai_quiz.py — 문제 생성과 채점](#7장-ai_quizpy)**
- **[8장. pdf_export.py — PDF 생성](#8장-pdf_exportpy)**
- **[9장. app.js — 프론트엔드 두뇌](#9장-appjs)**
- **[10장. ai_grade.js — 온디바이스 AI](#10장-ai_gradejs)**
- **[11장. script.js — 랜딩 스크롤 애니메이션](#11장-scriptjs)**
- **[12장. 함수 사전 — 특정 함수 심화 해설](#12장-함수-사전)**
- **[13장. 면접 대비 예상 질문과 답변](#13장-면접-대비)**
- **[부록. 파일 지도와 용어집](#부록-파일-지도와-용어집)**

---

## 머리말: 이 책을 읽는 법

각 함수 설명은 항상 같은 틀을 따릅니다.

> **파일명 → 줄 번호 → 하는 일 → 전체에서의 역할 → 쉬운 비유**

읽는 순서를 추천하면:

1. **0장(준비운동)** 을 먼저 읽어 용어에 익숙해집니다. (10분)
2. **1장(전체 구동 방식)** 으로 큰 그림을 잡습니다. ← 면접에서 제일 먼저 물어보는 부분입니다.
3. **2~11장** 은 파일별로 하나씩 봅니다.
4. **12장(함수 사전)** 은 "이 함수 자세히 설명해봐"라는 꼬리질문 대비용 심화편입니다.
5. **13장(예상 질문)** 으로 마무리 점검합니다.

⭐ 표시는 "면접에서 특히 강조하면 좋은, 프로젝트의 핵심 기술"입니다.

---

## 0장. 준비운동

> 코드를 보기 전에, 자주 나오는 단어 20개를 먼저 익혀둡시다. 이걸 알면 뒤 내용이 술술 읽힙니다.

| 용어 | 한 줄 뜻 | 쉬운 비유 |
|---|---|---|
| **함수(function)** | 이름 붙은 작업 묶음. 재료(입력)를 넣으면 결과(출력)를 냄 | 믹서기: 과일(입력) → 주스(출력) |
| **변수(variable)** | 값을 담아두는 이름표 붙은 상자 | 이름 적힌 서랍 |
| **프론트엔드(frontend)** | 사용자가 보는 화면. 브라우저에서 도는 자바스크립트 | 식당 홀 |
| **백엔드(backend)** | 화면 뒤에서 일하는 서버 프로그램(파이썬) | 주방 |
| **데이터베이스(DB)** | 정보를 영구 보관하는 곳 | 창고 |
| **API** | 프론트와 백엔드가 주고받는 약속된 창구(`/api/...`) | 주문서 양식 |
| **요청(request)/응답(response)** | 브라우저가 물어보고(요청) 서버가 답하는(응답) 것 | 주문 ↔ 음식 |
| **JSON** | 데이터를 글자로 정리한 표준 형식 `{"이름":"값"}` | 표 형식 메모지 |
| **비동기(async/await)** | 오래 걸리는 일을 기다리되 화면은 안 멈추는 방식 | 카톡 보내고 딴 일 하다 답 확인 |
| **스레드(thread)** | 동시에 일하는 "일꾼" 한 명 | 주방 보조 요리사 |
| **폴링(polling)** | "다 됐어?"를 반복해서 물어보기 | 진동벨 확인 |
| **콜백(callback)** | "이 일 끝나면 이걸 실행해줘"라고 미리 넘기는 함수 | 예약 알림 |
| **해시(hash)** | 되돌릴 수 없게 뒤섞기(단방향) | 과일 → 스무디(되돌리기 불가) |
| **토큰(token)** | 로그인 증명용 임시 열쇠 문자열 | 놀이공원 팔찌 |
| **쿠키(cookie)** | 브라우저가 서버에 매번 자동으로 들고 가는 쪽지 | 회원 팔찌를 늘 차고 다님 |
| **폴백(fallback)** | 실패 시 대신 작동하는 예비 방법 | 엘리베이터 고장 → 계단 |
| **싱글톤(singleton)** | 비싼 자원을 딱 하나만 만들어 재사용 | 백과사전 한 권 계속 참고 |
| **프롬프트(prompt)** | AI에게 주는 지시문 | 요리사에게 주는 레시피 |
| **localStorage** | 브라우저에 남는 작은 영구 메모장 | 냉장고에 붙인 포스트잇 |
| **정규식(regex)** | 글자 패턴을 검사하는 규칙 | "이 모양에 맞는지" 검사자 |

---

## 1장. 전체 구동 방식

> **면접 1순위 파트.** "이 서비스가 전체적으로 어떻게 돌아가나요?"라는 질문에 이 장 하나로 답할 수 있습니다.

### 1-1. 프로그램은 세 덩어리로 나뉜다

VoQuiz는 **단어장 PDF를 올리면 → AI가 단어를 뽑고 → 시험지를 만들고 → 학생이 풀면 AI가 채점**하는 웹 서비스입니다. 이 일을 셋이 나눠 합니다.

| 덩어리 | 정체 | 맡은 일 | 대표 파일 |
|---|---|---|---|
| **① 브라우저(프론트엔드)** | 자바스크립트 | 화면 그리기, 버튼, 진행바, 다크모드, 온디바이스 AI 채점 | `app.js`, `ai_grade.js`, `script.js` |
| **② 서버(백엔드)** | 파이썬 FastAPI | 로그인 확인, 파일 저장, AI 호출, DB 저장 | `app.py`, `auth.py`, `ai_quiz.py`, `ai_extract.py` |
| **③ 데이터베이스** | SQLite 파일 | 회원·단어·시험지·점수 영구 보관 | `database.py`, `data/vocashot.db` |

**비유:** 브라우저 = 손님이 보는 *식당 홀*, 서버 = *주방*, DB = *창고*. 손님(브라우저)이 주문(요청)하면 주방(서버)이 창고(DB)에서 재료를 꺼내 요리해 돌려줍니다. 이 주문↔응답 규칙이 **API**입니다.

### 1-2. 버튼 한 번 누르면 벌어지는 일 (요청의 일생) ⭐

학생이 **"로그인" 버튼**을 누르는 순간부터 화면에 대시보드가 뜰 때까지, 코드가 어떻게 이어달리기를 하는지 **한 편의 이야기**로 따라가 봅시다. 이 흐름을 이해하면 나머지 모든 기능이 똑같은 패턴임을 알게 됩니다.

```
[브라우저]                              [서버(app.py)]              [경비실(auth.py)]     [창고(database.py)]
   │  ①버튼 클릭 → doLogin()             │                            │                    │
   │  ②api("/api/login", 이메일·비번) ──▶│ ③login() 실행              │                    │
   │                                      │ ④get_user_by_email() ─────┼───────────────────▶│ ⑤회원 정보 조회
   │                                      │ ⑥verify_password() ──────▶│ (해시 비교)         │
   │                                      │ ⑦create_session() ───────▶│ ⑧토큰을 sessions에 저장 ▶│
   │  ⑩쿠키(session=토큰) 저장 ◀─────────│ ⑨set_cookie(토큰)          │                    │
   │  ⑪enterApp() → api("/api/me")  ────▶│ ⑫current_user()가 쿠키 검문 → 통과              │
   │  ⑬대시보드 화면 그리기 ◀────────────│ ⑭내 정보 JSON 응답         │                    │
```

**단계별 자세히:**

1. **①클릭 → `doLogin()`** (`app.js:232`): 입력창에서 이메일·비밀번호를 읽습니다.
2. **②`api()` 호출** (`app.js:18`): 모든 서버 통신이 거치는 단 하나의 통로. `fetch`로 서버에 요청을 보냅니다.
3. **③`login()`** (`app.py:165`): 서버가 요청을 받습니다.
4. **④~⑥ 신원 확인**: `auth.py`가 이메일로 회원을 찾고(`get_user_by_email` `auth.py:178`), 비밀번호를 **해시로 비교**합니다(`verify_password` `auth.py:74`).
5. **⑦~⑧ 세션 발급**: 임의의 긴 토큰을 만들어(`create_session` `auth.py:148`) DB `sessions` 표에 저장합니다.
6. **⑨~⑩ 쿠키 심기**: 서버가 그 토큰을 **쿠키**로 브라우저에 줍니다(`app.py:174`). 브라우저는 이 팔찌를 계속 차고 다닙니다.
7. **⑪~⑫ 재확인**: 이후 브라우저가 `/api/me`를 부르면, 서버의 문지기 `current_user()`(`app.py:84`)가 쿠키를 검문해 통과시킵니다.
8. **⑬~⑭ 화면 완성**: 서버가 내 정보를 JSON으로 주면, 브라우저가 그걸로 대시보드를 그립니다(`enterApp` `app.js:261`).

> **핵심 통찰:** 단어 추출·시험 생성·채점 등 **모든 기능이 이 "브라우저 → api() → 서버 함수 → auth 검문 → DB → 응답 → 화면 갱신" 패턴**을 그대로 따릅니다. 이 하나만 완벽히 이해하면 나머지는 응용입니다.

### 1-3. 데이터의 대장정: 가입부터 리포트까지

사용자가 겪는 전체 여정과, 각 단계가 건드리는 표(DB 테이블)입니다.

```
회원가입/이메일인증  ──▶  users 표, verification_codes 표
      │
   PDF 업로드          ──▶  서버 uploads/ 폴더에 파일 저장
      │
   단어 추출(AI+OCR)   ──▶  (임시) 진행바로 진행 상황 표시
      │
   단어장 저장          ──▶  wordbooks 표 + words 표
      │
   범위 선택 → 시험 생성 ──▶  (AI가 4지선다 생성)
      │
   시험지 저장          ──▶  exams 표 (문제를 JSON 문자열로 보관)
      │
   시험 응시(타이머)     ──▶  브라우저 메모리(takeState)에 답 임시 보관
      │
   AI 채점              ──▶  온디바이스 or Gemini
      │
   결과 저장            ──▶  attempts 표 (점수·정오표 보관)
      │
   대시보드/리포트/PDF   ──▶  attempts를 집계해 통계·차트·PDF 생성
```

**한 문장 요약:** *"업로드 → 추출 → 저장 → 생성 → 응시 → 채점 → 기록 → 집계"*, 이 8단계가 VoQuiz의 뼈대입니다.

### 1-4. 두 개의 AI (온디바이스 vs 서버) ⭐⭐

VoQuiz의 가장 독특한 설계입니다. 주관식 채점을 **두 방법 중 골라서** 합니다.

| | **온디바이스 AI** (`ai_grade.js`) | **서버 Gemini** (`ai_quiz.py`) |
|---|---|---|
| 어디서 도나 | 내 브라우저 안 (내 컴퓨터 GPU) | 구글 클라우드 서버 |
| 개인정보 | 답안이 밖으로 안 나감(비공개) | 답안이 구글로 전송됨 |
| 비용 | 무료·무제한 | 유료 → 하루 100문제 한도 |
| 조건 | WebGPU 지원 기기 + 1.4GB 모델 다운로드 | 항상 가능 |

**작동 규칙(폴백 3단계):**

```
주관식 채점 시:
  ① 온디바이스 모델이 준비돼 있으면 → 브라우저에서 채점 (gradeWrittenOnDevice)
        │ 실패하면 ▼
  ② 서버 Gemini로 채점 (하루 한도 안이면)          (_grade_written)
        │ 한도 초과 or 실패하면 ▼
  ③ 규칙 기반: 정답과 글자가 정확히 같으면 정답     (폴백)
```

이 3단계 덕분에 **AI가 고장 나든, 돈이 떨어지든, 서비스는 절대 멈추지 않습니다.** (자세한 코드는 7장·10장)

### 1-5. 진동벨 구조 (백그라운드 + 폴링) ⭐

단어 추출·시험 생성·채점은 **몇십 초**가 걸립니다. 그동안 화면이 얼어붙으면 안 되겠죠. 그래서 "진동벨" 방식을 씁니다.

```
[브라우저]                          [서버]
  요청: "추출 시작해줘"  ──────────▶ create_job() → job_id 발급
  ◀────────────────── job_id 즉시 반환 (일은 아직 안 끝남!)
                                     └▶ 별도 스레드가 진짜 추출 작업 시작
                                          중간중간 progress.update(진행률)
  0.6초마다: "다 됐어?" (job_id) ──▶ progress.get(job_id) → 45%
  ◀──────────────── "45% 진행 중"      진행바 갱신
  0.6초마다: "다 됐어?" ───────────▶ ... → 100%, 결과 포함
  ◀──────────────── "완료! 결과 여기"  화면에 결과 표시
```

- 서버 쪽: `extract_start`(`app.py:359`)가 스레드에 일을 맡기고 `job_id`만 즉시 반환.
- 브라우저 쪽: `pollProgress`(`app.js:1827`)가 0.6초마다 물어보며 진행바를 그림.
- 상태 저장소: `progress.py`의 `_jobs` 딕셔너리가 진행률을 계속 갱신.

**비유:** 오래 걸리는 요리를 주문하면 **진동벨**을 받고 자리로 돌아갑니다. 계산대에 붙잡아두지 않는 게 핵심입니다.

---

## 2장. app.py

**정체: 서버의 심장 (API 서버, 920줄) — "주방장".**
브라우저가 보내는 모든 주문(`/api/...`)을 받아 알맞은 부서에 넘기고 결과를 돌려줍니다. `@app.post(...)` / `@app.get(...)` 이 붙은 함수 하나가 "주문 창구 한 개"입니다.

### 2-1. `current_user()` — 로그인 검문소 ⭐
- **위치:** `app.py:84`
- **하는 일:** 쿠키에서 `session` 토큰을 꺼내(`:86`) 진짜 로그인한 사람인지 확인(`:87`). 아니면 즉시 "401 로그인 필요"를 던집니다(`:89`).
- **전체 역할:** 거의 모든 API가 `user=Depends(current_user)`를 붙이는데, 그러면 FastAPI가 **본체 함수 실행 전에 이 검문을 자동으로 먼저** 통과시킵니다. 내 데이터에 남이 못 들어오게 막는 **문지기**입니다.
- **비유:** 놀이공원 입구에서 팔찌를 확인하는 직원.

### 2-2. `signup()` / `verify()` / `login()` — 회원가입 3단계
- **위치:** 가입 `:112`, 인증 `:140`, 로그인 `:165`
- **하는 일:** ①가입 키·이메일·비번 검사 후 6자리 인증코드 발송(`:130`) → ②코드 확인 후 인증 도장 + 세션 쿠키(`:145~148`) → ③로그인 시 비번 대조 + 인증 여부 확인 후 쿠키(`:169~174`).
- **전체 역할:** 진짜 이메일 주인만 가입하게 해 스팸·가짜 계정을 막습니다.

### 2-3. `upload_file()` — 파일 받기
- **위치:** `app.py:336`
- **하는 일:** PDF/이미지를 받아 확장자 검사(`:339`) 후 `u3_1783822090.pdf`처럼 **누가·언제 올렸는지 담긴 안전한 이름**으로 저장(`:342~345`). PDF면 페이지 수를 세서 반환(`:346`).
- **전체 역할:** 단어 추출의 첫 단추.

### 2-4. `extract_start()` — 백그라운드 추출 시작 ⭐
- **위치:** `app.py:359`
- **하는 일:** `job_id` 발급(`:366`) → 무거운 추출은 `worker` 함수를 **별도 스레드**로 실행(`:371`, 시작 `:382`) → `job_id`만 즉시 반환(`:383`).
- **전체 역할:** 1-5의 진동벨 구조 서버 쪽. `generate_start`(`:494`), `grade_start`(`:605`)도 **완전히 같은 구조**입니다.
- **비유:** 진동벨을 손님에게 주고 요리는 주방에서 따로 진행.

### 2-5. `save_wordbook()` — 단어장 저장
- **위치:** `app.py:395`
- **하는 일:** 단어장 정보를 `wordbooks`에 한 줄(`:405`), 단어들을 `words`에 여러 줄 한꺼번에(`executemany` `:416`) 저장.
- **전체 역할:** 임시 추출 결과를 영구 보관으로 전환.

### 2-6. `grade_start()` — 채점 시작 (분기점) ⭐
- **위치:** `app.py:605`
- **하는 일:** 시험지·답안을 받아 `ai_quiz.grade(...)`에 채점 위임(`:621`). 이때 **브라우저가 미리 온디바이스로 채점한 결과(`ai_pregraded`)** 를 함께 받습니다. 채점 후 `attempts`에 저장하고 채점방식(`graded_by`)도 기록(`:622`).
- **전체 역할:** 시스템에서 가장 중요한 "AI 채점"의 서버 입구.

### 2-7. `get_stats()` — 대시보드 통계 집계
- **위치:** `app.py:276`
- **하는 일:** 단어장·단어·시험지 수, 평균/최고 점수, 이번 주 응시 수, 최근 기록을 **SQL 집계**(`COUNT`, `AVG`, `MAX`)로 한 번에 계산(`:280~303`).
- **전체 역할:** 대시보드의 모든 숫자·차트가 이 함수에서 나옵니다.

### 2-8. 학부모 공유 리포트 — `create_share()` / `public_report()`
- **위치:** 토큰 발급 `:687`, 공개 열람 `:713`
- **하는 일:** `secrets.token_urlsafe(12)`로 추측 불가능한 토큰 생성(`:694`). 부모님은 그 링크만으로 로그인 없이 자녀 진도 열람. 단 토큰이 DB에 있는지 확인(`:716`)해 아무 링크나 막습니다.
- **비유:** 구글 문서의 "링크 아는 사람만 보기". 링크가 곧 열쇠.

### 2-9. `_no_cache_static()` — 옛 화면 캐시 방지
- **위치:** `app.py:57`
- **하는 일:** HTML/JS/CSS에 "매번 새로 확인해" 헤더를 붙임(`:63`).
- **전체 역할:** 새로 배포했는데 브라우저가 옛 `app.js`를 기억해 쓰는 버그를 막습니다.

### 2-10. 맨 마지막 줄 — 정적 파일 연결
- **위치:** `app.py:920`
- **하는 일:** `/api/...`가 아닌 모든 주소는 `static/` 폴더의 실제 파일을 내보냄.
- **전체 역할:** 서버 하나가 API도, 화면도 둘 다 서빙. 그래서 프론트가 `/api/...` 상대경로만 써도 됩니다.

---

## 3장. auth.py

**정체: 로그인·비밀번호·세션·이메일 인증 담당 (260줄) — "경비실".**
면접에서 **보안 개념**을 묻기 딱 좋은 파일입니다.

### 3-1. `hash_password()` — 되돌릴 수 없게 저장 ⭐⭐
- **위치:** `auth.py:68`
- **하는 일:** 비밀번호를 그대로 저장하지 않습니다. ①사람마다 다른 **소금(salt)** 생성(`:70`) → ②비밀번호+소금을 **pbkdf2로 20만 번** 뒤섞음(`:71`) → ③`소금$해시`로 저장(`:72`).
- **왜 소금?** 소금이 없으면 "1234"는 항상 같은 해시 → 해커의 미리 만든 표(레인보우 테이블)로 뚫림. 소금을 섞으면 같은 비번도 결과가 전부 달라 무용지물이 됩니다.
- **왜 20만 번?** 일부러 느리게 만들어 무차별 대입 공격을 어렵게 합니다.
- **비유:** 소금 = 사람마다 다른 양념, 해시 = 믹서기에 갈기. 스무디를 과일로 못 되돌리듯 비번도 못 되돌립니다.

### 3-2. `verify_password()` — 상수시간 비교 ⭐
- **위치:** `auth.py:75`
- **하는 일:** 입력 비번을 같은 소금으로 해시해(`:81`) 저장값과 `hmac.compare_digest`로 비교(`:82`).
- **상수시간 비교란?** 보통 비교는 앞 글자가 틀리면 즉시 멈춥니다. 해커가 그 "멈추는 시간 차"를 재서 비번을 한 글자씩 알아낼 수 있는데(타이밍 공격), 이 함수는 항상 같은 시간이 걸려 막습니다.

### 3-3. `validate_password()` — 규칙 검사
- **위치:** `auth.py:46`
- **하는 일:** 6~15자, 영문·숫자·특수문자 1개 이상인지 **정규식**으로 확인(`:54~60`).
- **전체 역할:** 서버(`app.py:119`)와 브라우저(`checkPwRules`) 양쪽에서 검사하되, 브라우저 검사는 편의용이고 **서버 검사가 진짜 방어선**입니다(해커는 브라우저 검사를 건너뛸 수 있으니까).

### 3-4. `issue_verification_code()` / `check_verification_code()` — 이메일 인증
- **위치:** 발급 `:88`, 확인 `:101`
- **하는 일:** 6자리 난수 생성(`:90`) + 30분 유효(`:91`). 확인 시 만료(`:106`)·일치(`:108`)를 보고 성공하면 재사용 못 하게 즉시 삭제(`:110`).

### 3-5. `send_verification_email()` — 실제/개발 모드 발송
- **위치:** `auth.py:144`
- **하는 일:** SMTP 설정이 있으면 진짜 메일 발송(`:171~175`), 없으면 개발 모드로 콘솔에 코드만 출력(`:155`). (인증 메일은 브랜드 HTML 디자인으로 꾸며지며, 그 HTML은 `_verification_email_html()` `auth.py:114`에서 생성)
- **비유:** 연습 경기엔 심판이 말로만, 실제 경기엔 전광판에 띄우기.

### 3-6. `create_session()` / `get_user_by_token()` / `delete_session()` — 세션 ⭐
- **위치:** 생성 `:148`, 조회 `:159`, 삭제 `:170`
- **하는 일:** 로그인 성공 시 긴 임의 토큰 생성(`:150`) → DB 저장 → 쿠키로 전달. 이후 요청마다 쿠키가 따라오고 서버가 주인을 확인. 로그아웃하면 토큰 삭제.
- **왜 비밀번호 대신 토큰?** 비번을 매번 주고받으면 위험. 로그인은 한 번, 이후엔 임시 팔찌(토큰)로 통과.

---

## 4장. database.py

**정체: SQLite 표 정의 + 접근 도구 (380줄) — "창고 관리인".**
모든 정보를 `data/vocashot.db` 파일 하나에 표(테이블)로 보관합니다.

### 4-1. 왜 "관계형" 데이터베이스인가 ⭐
- **위치:** 스키마 `database.py:53~275`
- **핵심:** 정보를 여러 표로 나누고 **번호(id)로 연결**. `users`—`wordbooks`—`words`—`exams`—`attempts`. 예: 단어마다 `wordbook_id`가 붙어 "몇 번 단어장 소속인지" 가리킴(`:99`) → **외래키(FK)**.
- **전체 역할:** 회원 데이터가 안 섞이고(각 표에 `user_id`), "한 단어장에 단어 여러 개"(1:N)가 자연스럽게 표현됨.
- **비유:** 학생마다 학번을 주고 성적표·출석부를 학번으로 연결. 동명이인도 학번으로 구분.

### 4-2. `ON DELETE CASCADE` — 연쇄 삭제
- **위치:** 예 `database.py:75`, `:86`
- **하는 일:** 회원을 지우면 그 회원의 단어장·시험지·기록이 자동으로 함께 삭제.
- **비유:** 사물함을 없애면 안의 물건도 함께 치워짐.

### 4-3. `execute()` / `query_one()` / `query_all()` — DB 접근 3형제
- **위치:** `:347`, `:361`, `:368`
- **하는 일:** 저장/삭제(`execute`), 한 줄 읽기(`query_one`), 여러 줄 읽기(`query_all`). 결과를 파이썬 딕셔너리로 변환(`:365`).
- **`?` 자리표시자의 중요성(보안):** 값을 SQL 문자열에 직접 붙이지 않고 `?`에 따로 넣음(예 `app.py:280`) → **SQL 인젝션** 해킹 방지. 값이 명령어로 취급되지 않습니다.

### 4-4. `_write_lock` — 동시 저장 충돌 방지
- **위치:** 정의 `:28`, 사용 `:350`, `:377`
- **하는 일:** 여러 스레드가 동시에 DB에 쓸 때 한 번에 하나씩만 쓰게 자물쇠를 겁니다.
- **비유:** 화장실 문 잠금. 한 명이 쓰는 동안 밖에서 대기.

### 4-5. `_migrate()` — 표에 새 칸 추가 (데이터 보존)
- **위치:** `database.py:286`
- **하는 일:** 업데이트로 `nickname`·`avatar`·`explain_lang` 등 새 칸이 필요해지면, **기존 데이터를 지우지 않고** 없는 칸만 추가(`:296~342`).
- **전체 역할:** 이미 가입한 회원 정보를 지키며 기능을 늘리는 **마이그레이션**.
- **비유:** 이미 쓴 공책에 새 항목 칸만 뒤늦게 그려 넣되, 기존 내용은 그대로.

---

## 5장. progress.py

**정체: 진행바 상태 저장소 (92줄).**
"진동벨" 시스템의 상태판을 담당합니다.

### 5-1. `_jobs` — 인메모리 저장소
- **위치:** `progress.py:19`
- **하는 일:** 진행 중인 작업을 `{job_id: {진행률, 메시지, 로그, 결과...}}` 형태로 메모리에 보관.
- **비유:** 주방 벽 화이트보드. 요리 상태를 계속 지웠다 씀.

### 5-2. `create_job()` / `update()` / `finish()` / `fail()` / `get()`
- **위치:** `:23`, `:39`, `:56`, `:68`, `:79`
- **하는 일:** 작업 생성(고유 id 발급), 진행률·로그 갱신, 성공 마감, 실패 기록, 상태 읽기.
- **`_lock` 사용:** 여러 스레드가 동시에 화이트보드를 고치다 엉키지 않게 자물쇠를 씁니다.
- **왜 메모리?** 진행 상태는 잠깐 쓰고 버리는 정보라 DB까지 갈 필요 없음. 서버 재시작 시 사라져도 무방.

---

## 6장. ai_extract.py

**정체: PDF/사진 → 단어 추출 (244줄). OCR(글자 인식) + Gemini.**

### 6-1. `extract_words()` — 추출 총괄 + 페이지 병렬 처리 ⭐
- **위치:** `ai_extract.py:162`
- **하는 일:** ①PDF를 페이지별 이미지로 변환(300dpi로 또렷하게 `:183`) → ②**여러 페이지를 동시에** AI로 전송(`ThreadPoolExecutor(max_workers=5)` `:196`, 최대 5페이지 병렬) → ③끝날 때마다 진행바 갱신(`:208`) → ④순서대로 정렬·순번 부여(`:223`).
- **병렬 처리 비유:** 시험지 100장을 선생님 1명이 채점하면 오래 걸리지만 5명이 나누면 5배 빠름. 페이지들은 서로 무관해 동시에 처리 가능.

### 6-2. `_gemini_extract_image()` — 이미지 한 장을 AI에게
- **위치:** `ai_extract.py:117`
- **하는 일:** 이미지를 **base64 문자열로 인코딩**(`:106`)해 Gemini에 전송, "단어 목록을 JSON으로" 요청(`:126~132`). `temperature: 0.1`로 일관성 확보(`:131`).
- **base64란?** 그림 같은 이진 데이터를 글자로만 표현하는 방식. 인터넷은 글자를 주고받기 편하므로 그림을 글자로 변환해 전송.

### 6-3. 프롬프트 — `PROMPT_EN` / `PROMPT_ZH` ⭐
- **위치:** 영어 `:30`, 중국어 `:57`
- **하는 일:** AI에게 "사전 편집자처럼 행동하라"고 구체적으로 지시. 핵심 규칙(`:36~44`): 정의가 있으면 **그 정의를 한국어로 번역**(단어만 번역 X), 정의가 없으면 사전처럼 뜻·예문을 생성.
- **전체 역할:** "Bank"가 문맥상 '둑'인지 '은행'인지 구별하게 만드는 것이 이 지시문 → **프롬프트 엔지니어링**의 실제 예시.

### 6-4. `_finalize()` — 결과 청소
- **위치:** `ai_extract.py:228`
- **하는 일:** 단어가 빈 항목 제거(`:232`), 공백 정리, 순번(`seq`) 부여.

---

## 7장. ai_quiz.py

**정체: 문제 생성 + 채점 (392줄). ⭐ 프로젝트에서 가장 중요한 파일.**

### 7-1. `generate_quiz()` — 시험 문제 만들기
- **위치:** `ai_quiz.py:131`
- **하는 일:** ①단어를 섞어 필요한 수만 선택(`random.shuffle` `:145`) → ②**주관식**은 AI 없이 코드로 직접 생성(`:159`, 단순 형식이라) → ③**객관식**은 Gemini에게 "이 단어들로 4지선다를" 요청(`:190`, `:210`) → ④검증·정리 후 각 문제를 원본 단어와 짝지음(`_match_source_word` `:233`).

### 7-2. `grade()` — 채점 총괄 ⭐
- **위치:** `ai_quiz.py:292`
- **하는 일:** ①**객관식**은 답이 정해져 있어 코드로 정확 비교(`:308~320`, 빠르고 공짜) → ②**주관식**은 AI가 "뜻이 비슷하면 정답" 판단(`:322~349`) → ③여기서 **분기**(`:333`): `ai_pregraded`(온디바이스 결과)가 있으면 그걸(`ondevice`), 없으면 Gemini(`server`) → ④백분율 점수 계산(`:351~352`).
- **비유:** 객관식 = OMR 기계 채점, 주관식 = 선생님이 서술형을 읽고 의미로 채점.

### 7-3. `_grade_written()` — 주관식 채점 + 3중 안전장치 ⭐⭐
- **위치:** `ai_quiz.py:358`
- **하는 일:** ①**하루 한도** 확인(`:361`, Gemini는 유료라 하루 100문제 `GEMINI_DAILY_LIMIT` `:38`) → ②한도 안이면 Gemini 채점(`:378`), 성공 시 사용량 기록(`:380`) → ③**한도 초과 또는 실패 시** 단순 문자열 비교로 **폴백**(`:384~392`).
- **전체 역할:** AI가 실패해도, 돈이 떨어져도 **서비스가 절대 멈추지 않게** 하는 방어 설계.
- **사용량 기록:** `_gemini_usage_today()`(`:43`)·`_gemini_add_usage()`(`:56`)가 `gemini_usage.json`에 "오늘 몇 문제"를 날짜와 함께 저장, 날짜 바뀌면 0으로 리셋.

### 7-4. `_gemini_json()` — AI 응답 안전 파싱
- **위치:** `ai_quiz.py:99`
- **하는 일:** Gemini에 프롬프트 전송(`:107`) → AI가 붙이는 ```` ```json ```` 껍데기 제거(`:113`) → 진짜 JSON만 파싱(`:114`).

### 7-5. `check_usage()` — 예문 첨삭 AI
- **위치:** `ai_quiz.py:257`
- **하는 일:** 학생이 쓴 예문을 AI 선생님이 "문맥·문법에 맞게 썼는지" 판단하고 따뜻한 피드백 제공.
- **전체 역할:** 단순 암기를 넘어 "직접 써보고 교정받는" 능동 학습 지원.

---

## 8장. pdf_export.py

**정체: 시험지·리포트 PDF 생성 (356줄).**

### 8-1. `_register_fonts()` — 한글 폰트 등록
- **위치:** `pdf_export.py:27`
- **하는 일:** PDF 라이브러리는 기본적으로 한글을 모름 → 프로젝트의 나눔스퀘어라운드 TTF 등록(`:35`), 명조·고딕은 내장 한중일 폰트 사용(`:41~44`). 한 번만 등록하게 캐시(`_FONTS_READY` `:29`).
- **전체 역할:** PDF에서 한글이 네모(□□□)로 깨지지 않게 하는 필수 준비.

### 8-2. `_wrap_w()` — 줄바꿈 직접 계산
- **위치:** `pdf_export.py:56`
- **하는 일:** 글자가 종이 폭을 넘으면 줄바꿈. 실제 픽셀 폭을 `stringWidth`로 재가며(`:62`) 한글은 글자 단위, 영어는 단어 단위로 끊음.
- **전체 역할:** 웹과 달리 PDF는 자동 줄바꿈이 없어 직접 계산해야 함.

### 8-3. `create_quiz_pdf()` / `create_report_pdf()` / `create_progress_pdf()`
- **위치:** `:128`, `:252`, `:300`
- **하는 일:** 각각 시험지, 채점 리포트, 진도 리포트를 A4 캔버스에 좌표를 지정해가며 그림.
- **전체 역할:** 선생님이 인쇄해 나눠주거나 부모님께 보여줄 종이 산출물 생성.

---

## 9장. app.js

**정체: 프론트엔드 두뇌 (2465줄).**
화면 전환·버튼·진행바·다크모드를 모두 담당하는 바닐라(순수) 자바스크립트.

### 9-1. `api()` — 서버와 대화하는 단 하나의 통로 ⭐
- **위치:** `app.js:18`
- **하는 일:** 모든 서버 요청이 이 함수를 거침. `fetch`로 전송(`:26`), **`credentials: "include"`** 로 세션 쿠키를 꼭 함께 보냄(`:19`). 응답을 JSON으로 바꾸고 실패 시 오류를 던짐(`:29`).
- **`async/await`란?** 서버 응답은 시간이 걸림. `await`는 "응답 올 때까지 기다렸다 다음 줄 실행"이지만 **화면은 안 멈춤**. 다른 일 하다 응답 오면 이어감.
- **비유:** 카톡 보내고(요청) 멍 때리지 않고 딴 일 하다 답 오면(await) 확인.

### 9-2. `pollProgress()` — 진동벨 확인 ⭐
- **위치:** `app.js:1827`
- **하는 일:** `job_id`로 **0.6초마다** 진행 상황을 반복 요청(`while(true)`+`sleep(600)` `:1829~1830`) → 진행률·로그로 진행바 갱신(`:1834~1838`) → `done`이면 결과 반환(`:1839`), `error`면 오류(`:1840`).
- **전체 역할:** 2-4의 백그라운드 작업과 짝을 이루는 브라우저 절반. **폴링** = "다 됐어?" 반복.

### 9-3. `submitExam()` — 시험 제출 & 온디바이스 채점 연결 ⭐
- **위치:** `app.js:1579`
- **하는 일:** ①타이머 정지 + 주관식 답 모으기(`:1586~1591`) → ②**온디바이스 준비 시** 브라우저에서 먼저 채점(`:1597`) → ③그 결과를 서버로 함께 전송(`:1602`) → ④`pollProgress`로 완료 대기 후 리포트 렌더(`:1604~1606`).
- **전체 역할:** "브라우저 AI 채점 → 서버 저장" 하이브리드 흐름의 지휘자.

### 9-4. `startTimer()` / `stopTimer()` — 시험 타이머
- **위치:** `app.js:1556`, `:1573`
- **하는 일:** `setInterval`로 1초마다 시간 +1(`:1558`). 제한시간 모드면 남은 시간 계산 후 0이면 자동 제출(`:1564`), 30초 이하면 빨간 경고(`:1566`).
- **왜 멈춰야 하나?** 화면을 벗어나도 타이머가 돌면 메모리 낭비·오작동 → 화면 전환마다 정리(`showView` `:42`).

### 9-5. 다크모드 — `initTheme()` / `applyTheme()` / `toggleTheme()` ⭐
- **위치:** `:1891`, `:1897`, `:1907`
- **하는 일:** ①`initTheme`: 저장된 테마(`localStorage`)를 먼저 확인(`:1893`), 없으면 **OS 설정**(`prefers-color-scheme`)을 따름(`:1894`) → ②`applyTheme`: 최상단 태그에 `data-theme="dark"` 표시를 붙임(`:1898`) → **CSS가 이 표시를 보고 색을 통째로 교체** + 아이콘 해/달 전환(`:1900`) + 로고 교체(`:1904`) → ③`toggleTheme`: 뒤집고(`:1908`) localStorage에 저장(`:1909`).
- **핵심:** JS가 색을 하나씩 안 바꿈. **딱 한 글자(`data-theme`)** 만 바꾸면 CSS 변수들이 화면 전체 색을 한꺼번에 갈아끼움.
- **비유:** 전구를 하나씩 갈지 않고 **메인 스위치 하나**를 '밤 모드'로 돌리면 방 전체가 바뀜. 그 위치를 포스트잇(localStorage)에 적어둬 다음에도 그대로.

### 9-6. `ICONS` / `applyIcons()` — 색이 자동으로 맞는 아이콘
- **위치:** 정의 `:1849`, 적용 `:1863`
- **하는 일:** 아이콘을 SVG(그림 코드) 문자열로 저장, `stroke="currentColor"`(`:1848`)로 **글자색을 따라가게** 함 → 다크모드 시 아이콘도 자동으로 흰색.

### 9-7. `mdToHtml()` / `esc()` — 마크다운 변환 + XSS 방지 ⭐
- **위치:** `:2044`, `:2419`
- **하는 일:** AI 답변의 `**굵게**`·`- 목록`을 HTML로 변환하되, **먼저 `esc()`로 위험 문자를 무력화한 뒤**(`:2045`) 변환.
- **XSS란?** 답변에 악성 스크립트를 심으면 실행돼 해킹될 수 있음. `esc()`가 `<` 같은 문자를 무해한 기호로 바꿔 막음. 사용자 입력은 항상 `textContent`로 그대로 넣어(`:2037`) 실행 방지.

### 9-8. 학습 기능들 — 플래시카드·게임
- **위치:** 플래시카드 `startFlashcardsWith` `:466`, 예문첨삭 `checkSentence` `:497`, 게임 `startGame` `:531`
- **하는 일:** 추출한 단어로 플래시카드·짝맞추기·스피드퀴즈·스펠링 등 다양한 학습 모드 제공.
- **`fcMove()`의 똑똑한 트릭(`:516`):** `(idx + dir + n) % n` — **나머지 연산(%)** 으로 마지막 다음은 처음으로, 처음 이전은 마지막으로 순환. (자세히는 12장)

---

## 10장. ai_grade.js

**정체: 온디바이스 AI (203줄). ⭐⭐ 가장 자랑할 기술.**
"내 컴퓨터 안에서 직접 도는 AI". 답안이 인터넷으로 안 나가 **비공개·무료·무제한**.

### 10-1. `checkGPUSupport()` — 이 기기가 AI를 돌릴 수 있나?
- **위치:** `ai_grade.js:6`
- **하는 일:** **WebGPU**(그래픽 카드를 웹에서 쓰는 기술)가 있는지 확인(`navigator.gpu` `:8`, 실제 어댑터 요청 `:9`).
- **비유:** 무거운 짐을 들기 전 "지게차(GPU)가 있나?" 확인.

### 10-2. `getEngine()` — 모델을 딱 한 번만 로딩 (싱글톤) ⭐
- **위치:** `ai_grade.js:63`
- **하는 일:** ①이미 로딩된 엔진이 있으면 **재사용**(`:64`) → ②없으면 1.4GB Gemma 모델 다운로드(`CreateMLCEngine` `:71`), 진행률 표시(`:72`) → ③로딩 중 **취소** 지원(`Promise.race`로 로딩 완료 vs 취소 경쟁 `:74`) → ④성공 시 저장 + "완료" 표시(`:82~83`).
- **싱글톤이란?** 만들기 비싼 자원(1.4GB!)을 딱 하나만 만들어 재사용. 매번 새로 받으면 큰일.
- **비유:** 무거운 백과사전을 한 번 꺼내두고 계속 참고. 볼 때마다 새로 안 빌림.

### 10-3. `gradeWrittenOnDevice()` — 브라우저 안 채점 ⭐
- **위치:** `ai_grade.js:153`
- **하는 일:** 엔진 획득(`:155`, 없으면 `null`→서버 폴백) → 프롬프트 생성(`:161`) → 모델 전송(`:165`). **`temperature: 0`** 이 핵심(`:167`).
- **temperature란?** AI의 "창의성/무작위성" 손잡이. 1=매번 다른 답, **0=거의 항상 같은 답**. 채점은 같은 답에 항상 같은 점수를 줘야 공정하므로 0으로 고정(`:168~169` 주석).

### 10-4. `extractJson()` — AI 응답에서 JSON만 정밀 추출
- **위치:** `ai_grade.js:120`
- **하는 일:** 작은 모델은 JSON 앞뒤에 잡말을 붙임 → 첫 `[`부터 **괄호 짝을 세어** 짝 맞는 `]`까지만 추출(`:126~136`). 문자열 안 대괄호는 세지 않게 따옴표 상태도 추적(`:129~135`).
- **비유:** 편지에서 필요한 표(대괄호 부분)만 정확히 오리되, 표 안에 그려진 괄호에 헷갈리지 않기.

### 10-5. `_llmFriendlyError()` — 오류를 사람 말로 번역
- **위치:** `ai_grade.js:42`
- **하는 일:** 기술 오류를 종류별로 알아보고("429 요청 과다", "GPU 문제") 쉬운 안내로 변환(`:44~48`) + "그동안 서버 Gemini로 채점된다"고 안심.

### 10-6. `window.___ = ...` — 함수를 다른 파일에 공개 ⭐
- **위치:** `ai_grade.js:146~151`, `:203`
- **하는 일:** `ai_grade.js` 안 함수는 그 파일에 **갇혀 있어** `app.js`가 못 봄 → `window.gradeWrittenOnDevice = ...`로 **전역에 올려** 공개(`:146` 주석).
- **스코프(scope)란?** 변수·함수가 "보이는 범위". 함수 안 변수는 밖에서 안 보임. `window`는 모든 파일이 공유하는 "공용 게시판".
- **비유:** 내 방(파일) 안 물건은 나만 쓰지만, 거실 공용 테이블(window)에 올리면 온 가족이 씀.

### 10-7. `chatOnDevice()` + `_reportDevice()` — AI 튜터 & 계측
- **위치:** 튜터 `:189`, 계측 `:52`
- **하는 일:** 같은 온디바이스 엔진으로 단어 학습 질문에 답하는 AI 튜터 제공(단어 학습만 답하도록 시스템 프롬프트로 제한 `:184`). 별도로 기기의 WebGPU 지원·로딩 성공·소요시간을 서버에 기록(연구용 계측).

---

## 11장. script.js

**정체: 랜딩(소개) 페이지 스크롤 애니메이션 (137줄).**
스크롤에 따라 기기가 펴지고 장면이 전환되는 애플 스타일. 부드럽고 배터리를 아끼는 기법이 핵심.

### 11-1. 수학 도구 3형제 — `clamp` / `lerp` / `range`
- **위치:** `script.js:9~12`
- **하는 일:** `clamp`(값을 0~1로 가둠 `:9`), `lerp`(두 값 사이 부드럽게 이음 `:10`), `range`(특정 스크롤 구간 진행도 계산 `:12`).
- **비유:** `clamp`=볼륨 0~100 제한, `lerp`=두 지점 사이 매끄러운 슬라이드.

### 11-2. `IntersectionObserver` — 보이면 등장 ⭐
- **위치:** 등장 `:18`, 숫자 카운트업 `:38`
- **하는 일:** 요소가 화면에 들어오는 순간을 감지해 `in` 클래스로 나타냄(`:20`), 한 번 나타나면 감시 종료(`unobserve` `:21`). 카운트업은 숫자가 보이면 1.4초간 세어 올림, `easeOutCubic`(`:44`)으로 처음 빠르고 끝에 느리게.
- **비유:** 문에 센서를 달아 사람이 들어오면 자동으로 불이 켜지는 것. 계속 지켜볼 필요 없음.

### 11-3. `update()` + `requestAnimationFrame` — 스크롤 연동의 심장 ⭐⭐
- **위치:** 계산 `:69`, 스크롤 처리 `onScroll` `:111`
- **하는 일:** ①상단 진행바를 스크롤 비율만큼 채움(`:74~75`) → ②내비가 어두운 섹션 위에 오면 색 반전(`:79~84`) → ③**핀 고정 구간**: 화면을 고정한 채 스크롤을 3등분해 3장면 전환(`:90~106`).
- **성능 최적화(핵심):** 스크롤은 초당 수백 번 발생 → 매번 계산하면 버벅임 → **`requestAnimationFrame`** 으로 "화면 다시 그리기 직전에 딱 한 번만" 계산(`:114`). `ticking` 깃발(`:110~113`)로 중복 방지.
- **비유:** 사진사가 아무 때나 셔터를 누르지 않고, **화면 갱신 순간(초당 60번)에 딱 맞춰** 한 번만 찍음. 헛수고 없음.
- **접근성:** `prefers-reduced-motion`(`:14`)이 켜져 있으면 화려한 효과를 끔.

### 11-4. `isMobile()` — 반응형 분기
- **위치:** 정의 `:15`, 사용 `:35`, `:86`
- **하는 일:** 폭 960px 이하(모바일)면 무거운 핀 고정 대신 **장면을 스크롤 시 하나씩 등장**시키는 가벼운 방식 사용(`:35`).

---

## 12장. 함수 사전

> **꼬리질문 대비 심화편.** "이 함수 한 줄씩 설명해봐"라는 질문에 대비해, 핵심 함수들을 **시그니처·입력·출력·한 줄 해설·왜 이렇게 짰나·연결 함수**로 깊이 팠습니다.

### 사전 1. `api(path, options)` — 프론트엔드의 만능 통신 함수
- **위치:** `app.js:18` | **시그니처:** `async function api(path, { method, body, isForm })`
- **입력:** 주소(`path`), 방식(GET/POST 등), 보낼 데이터(`body`)
- **출력:** 서버가 준 JSON(파이썬 dict → JS 객체)
- **한 줄 해설:**
  - `:19` 요청 옵션을 만들며 `credentials:"include"`로 **쿠키 자동 첨부** (로그인 유지의 필수 조건)
  - `:20~25` 파일이면 FormData 그대로, 아니면 JSON 문자열로 변환해 담음
  - `:26` `fetch`로 실제 전송(여기서 `await`로 응답 대기)
  - `:28` 응답을 JSON으로 파싱(실패해도 터지지 않게 try로 감쌈)
  - `:29` 서버가 오류(4xx/5xx)면 오류 메시지를 뽑아 `throw` → 부른 쪽의 `catch`가 처리
- **왜 이렇게?** 통신 코드를 한 곳에 모으면, 쿠키 첨부·오류 처리를 40여 개 API마다 반복하지 않아도 됨(**DRY 원칙**: 반복하지 마라).
- **연결:** 거의 모든 함수(`doLogin`, `startExtract`, `submitExam`, `loadStats`...)가 이걸 부름.

### 사전 2. `current_user(request)` — 서버의 문지기
- **위치:** `app.py:84` | **시그니처:** `def current_user(request: Request)`
- **입력:** 들어온 HTTP 요청(쿠키 포함) | **출력:** 로그인한 사용자 정보(dict) 또는 401 오류
- **한 줄 해설:**
  - `:86` 요청 쿠키에서 `session` 토큰을 꺼냄
  - `:87` `auth.get_user_by_token`으로 "이 토큰 주인이 누구?" 조회
  - `:88~89` 주인이 없으면(토큰이 가짜/만료) 즉시 401 던짐
- **왜 이렇게?** FastAPI의 `Depends(current_user)`에 넣으면, 보호가 필요한 함수마다 검문 코드를 다시 쓰지 않고 **선언 한 줄**로 재사용. 보안 로직을 한 곳에 모아 실수를 줄임.
- **연결:** `me`, `get_stats`, `upload_file`, `grade_start` 등 로그인 필요한 거의 모든 API.

### 사전 3. `hash_password(password)` & `verify_password(password, stored)` — 비밀번호 금고 ⭐
- **위치:** `auth.py:67`, `:74`
- **hash 한 줄 해설:** `:69` 랜덤 소금 생성 → `:70` `pbkdf2_hmac("sha256", 비번, 소금, 200000)` 20만 번 반복 → `:71` `소금$해시`로 합쳐 반환
- **verify 한 줄 해설:** `:77` 저장값을 `소금`과 `해시`로 분리 → `:80` 입력 비번을 **같은 소금**으로 똑같이 해시 → `:81` `compare_digest`로 상수시간 비교
- **왜 이렇게?** ①소금 → 레인보우 테이블 방어 ②반복 20만 → 무차별 공격 지연 ③상수시간 비교 → 타이밍 공격 방어. 세 겹 방어.
- **연결:** `signup`/`login`(app.py), `create_user`/`update_password`(auth.py).

### 사전 4. `create_session(user_id)` — 로그인 팔찌 발급
- **위치:** `auth.py:148` | **입력:** 사용자 id | **출력:** 세션 토큰(문자열)
- **한 줄 해설:** `:150` `secrets.token_urlsafe(32)`로 추측 불가능한 긴 토큰 생성 → `:151` `sessions` 표에 (토큰, user_id, 시각) 저장 → 반환
- **왜 `secrets`?** 일반 랜덤(`random`)은 예측 가능해 보안에 부적합. `secrets`는 **암호학적으로 안전한** 난수라 토큰·인증코드·공유링크에 사용.
- **연결:** 발급 후 `app.py`가 쿠키로 심고, `get_user_by_token`이 검증에 사용.

### 사전 5. `extract_start(body, user)` — 백그라운드 작업의 표준형 ⭐
- **위치:** `app.py:359`
- **한 줄 해설:**
  - `:362~364` 업로드된 파일이 실제로 있는지 확인
  - `:366` `progress.create_job()`으로 작업표 발급(job_id)
  - `:368~369` 진행상황을 `progress.update`에 전달할 `report` 콜백 정의
  - `:371~380` `worker()`: 실제 추출을 수행하고 성공 시 `finish`, 실패 시 `fail`
  - `:382` `threading.Thread(...).start()` — worker를 **별도 스레드**로 실행(서버는 안 멈춤)
  - `:383` job_id를 즉시 반환
- **왜 이렇게?** 추출은 수십 초 → 응답을 기다리게 하면 브라우저가 멈춘 것처럼 보이고 타임아웃 위험. 그래서 **일은 뒤에서, 번호표는 앞에서 즉시**.
- **연결:** `generate_start`·`grade_start`가 똑같은 틀을 사용 / 브라우저의 `pollProgress`가 짝.

### 사전 6. `extract_words(...)` — 페이지 병렬 처리 ⭐
- **위치:** `ai_extract.py:162`
- **한 줄 해설:**
  - `:183` `convert_from_path(dpi=300, ...)` PDF를 페이지별 이미지로
  - `:196` `ThreadPoolExecutor(max_workers=5)` 최대 5명의 일꾼 준비
  - `:197~200` 각 페이지를 일꾼에게 배정(`executor.submit`)
  - `:201~218` **먼저 끝나는 페이지부터**(`as_completed`) 결과를 모으고 진행바 갱신
  - `:223` 페이지 순서대로 정렬(병렬이라 순서가 섞였을 수 있음)
- **왜 병렬?** 페이지들은 서로 독립적 → 동시에 처리하면 5배 빠름. 단, 결과가 뒤섞여 오므로 마지막에 `sort`로 순서 복원.
- **연결:** `extract_start`의 worker가 호출 / `_process_page`·`_gemini_extract_image`를 부름.

### 사전 7. `grade(quiz, user_answers, report, ai_pregraded)` — 채점 지휘자 ⭐
- **위치:** `ai_quiz.py:292`
- **한 줄 해설:**
  - `:304` 주관식 문항 번호만 골라둠
  - `:308~320` **객관식**은 반복문으로 정답과 정확 비교(로컬·즉시)
  - `:323~349` **주관식**이 있으면 채점: `:333` **분기** — `ai_pregraded`(온디바이스 결과)가 문항 수와 맞으면 그걸 쓰고 `graded_by="ondevice"`, 아니면 `_grade_written`(Gemini) 호출 `graded_by="server"`
  - `:351~352` 맞은 수 / 전체 × 100 = 백분율 점수
- **왜 분리?** 객관식은 정답이 하나라 AI가 필요 없음(공짜·정확). 주관식만 AI로 "의미가 통하면 정답" 판단 → 비용과 정확성의 균형.
- **연결:** `grade_start`가 호출 / `_grade_written`으로 폴백.

### 사전 8. `_grade_written(payload)` — 3중 폴백의 실체 ⭐⭐
- **위치:** `ai_quiz.py:358`
- **한 줄 해설:**
  - `:361` 오늘 사용량 + 이번 요청이 하루 한도를 넘는지 계산
  - `:362~383` 한도 안이면 Gemini 시도: 프롬프트 생성 → `:378` 모델 호출 → `:379~381` 성공(list 반환)이면 사용량 기록 후 반환
  - `:384~392` **한도 초과 or 실패** 시 폴백: 정답과 글자가 정확히 같으면 정답 처리, 한도 초과면 "온디바이스 권장" 안내 추가
- **왜 이렇게?** 유료 API의 비용 폭주를 막고(한도), 장애에도 결과를 보장(폴백). "완벽하진 않아도 항상 답은 준다"는 신뢰성 설계.
- **연결:** `grade`가 서버 채점 경로에서 호출.

### 사전 9. `pollProgress(jobId, prog)` — 진동벨 확인 루프 ⭐
- **위치:** `app.js:1827`
- **한 줄 해설:**
  - `:1829` 무한 반복 시작(`while(true)`)
  - `:1830` `await sleep(600)` 0.6초 쉼(서버를 과하게 두드리지 않게)
  - `:1832` 진행 상황 조회(실패하면 다음 회차로 넘어감 `:1833`)
  - `:1834~1838` 진행률·새 로그를 화면에 반영(`seenLogs`로 이미 본 로그는 건너뜀)
  - `:1839~1840` `done`이면 결과 반환하며 루프 종료, `error`면 오류 던짐
- **왜 폴링?** 서버가 먼저 알려주는 방식(웹소켓)은 구현이 복잡. 진행바 정도는 "주기적으로 물어보기"가 단순하고 충분.
- **연결:** `startExtract`·`generateExam`·`submitExam`이 공통으로 사용.

### 사전 10. `submitExam()` — 하이브리드 채점의 지휘 ⭐
- **위치:** `app.js:1579`
- **한 줄 해설:**
  - `:1580` 타이머 정지
  - `:1586~1591` 주관식 답안만 골라 payload 구성
  - `:1596~1598` **온디바이스가 준비됐고 주관식이 있으면** 브라우저에서 먼저 채점
  - `:1602~1603` 답안 + 온디바이스 결과(`ai_pregraded`)를 서버로 전송
  - `:1604` `pollProgress`로 서버 저장 완료 대기
  - `:1605~1606` 응시 id 기억 후 결과 리포트 렌더
- **왜 브라우저에서 먼저?** 온디바이스로 채점하면 답안이 서버로 안 가 **개인정보 보호** + 서버 비용 0. 서버는 결과를 받아 "저장"만 담당.
- **연결:** `gradeWrittenOnDevice`(ai_grade.js) + `grade_start`(app.py) 사이의 다리.

### 사전 11. `getEngine(showError)` — 싱글톤 + 취소 가능한 로딩 ⭐
- **위치:** `ai_grade.js:63`
- **한 줄 해설:**
  - `:64` **이미 있으면 즉시 재사용**(싱글톤 핵심)
  - `:65` GPU 지원 확인
  - `:70` 취소용 Promise 생성(취소 버튼이 이걸 완료시킴)
  - `:71~73` 1.4GB 모델 다운로드 시작, 진행률 콜백으로 로딩바 갱신
  - `:74` `Promise.race([로딩, 취소])` — **둘 중 먼저 끝나는 것**을 채택
  - `:77~81` 취소면 null 반환(뒤늦게 끝나면 메모리 해제), 완료면 `engine`에 저장
- **왜 `Promise.race`?** "로딩 완료"와 "사용자 취소"를 동시에 기다리다 먼저 오는 쪽을 처리 → 큰 다운로드를 중간에 멈출 수 있게.
- **연결:** `gradeWrittenOnDevice`·`chatOnDevice`·`prepareOnDeviceAI`가 모두 이걸 통해 엔진 획득.

### 사전 12. `gradeWrittenOnDevice(payload)` — 온디바이스 채점 ⭐
- **위치:** `ai_grade.js:153`
- **한 줄 해설:**
  - `:155~158` 엔진 획득, 없으면 `null`(→서버 폴백)
  - `:161` 채점 프롬프트 생성
  - `:165~170` 모델 호출, **`temperature:0`** 으로 재현성 확보
  - `:171~173` 응답에서 `extractJson`으로 JSON만 뽑아 파싱
  - `:175~178` 실패 시 콘솔에 이유 남기고 `null` 반환(→서버 폴백)
- **왜 `null` 반환 설계?** "실패=null"로 통일하면 부른 쪽(`submitExam`)이 간단히 "null이면 서버로"라고 처리 가능 → 폴백이 자연스럽게 연결.
- **연결:** `submitExam`이 호출 / 실패 시 서버 `_grade_written`이 이어받음.

### 사전 13. `extractJson(text)` — 괄호 짝 세기 파서
- **위치:** `ai_grade.js:120`
- **한 줄 해설:**
  - `:121` 마크다운 코드펜스 제거
  - `:122~123` 첫 `[` 위치 찾기(없으면 그대로 반환)
  - `:126~136` `[`부터 스캔하며 **깊이(depth)** 를 세되, 문자열 안(`inStr`)의 대괄호는 무시(`\`로 이스케이프된 따옴표도 처리)
  - `:135` 깊이가 0으로 돌아오는 순간의 `]`가 진짜 끝
  - `:138` 후행 콤마(`},]`) 제거
- **왜 이렇게?** 작은 모델은 JSON 뒤에 설명을 덧붙이거나 문자열 안에 `]`를 넣기도 함 → 단순히 "마지막 `]`"를 찾으면 틀림. 그래서 **괄호 균형**을 직접 셈. (컴파일러가 괄호를 검사하는 원리와 동일)
- **연결:** `gradeWrittenOnDevice`가 응답 파싱에 사용.

### 사전 14. `applyTheme(theme)` — 한 글자로 전체 색 바꾸기 ⭐
- **위치:** `app.js:1897`
- **한 줄 해설:**
  - `:1898` `<html>`에 `data-theme="dark"|"light"` 설정 ← **이 한 줄이 핵심**
  - `:1900~1902` 토글 버튼 아이콘을 해/달로 교체
  - `:1904~1905` 로고 이미지를 밝은판/어두운판으로 교체
- **왜 한 글자?** CSS에 `[data-theme="dark"] { --배경: 검정; ... }`처럼 색 변수를 미리 정의 → JS는 표시만 바꾸고 **실제 색칠은 CSS가** 담당. 역할 분리로 코드가 단순해짐.
- **연결:** `initTheme`(첫 적용)·`toggleTheme`(전환)이 호출.

### 사전 15. `update()` — rAF로 모든 스크롤 효과 계산 ⭐
- **위치:** `script.js:69` (호출 관리 `onScroll` `:111`)
- **한 줄 해설:**
  - `:70~71` 현재 스크롤 위치·화면 높이 읽기
  - `:74~75` 상단 진행바를 스크롤 비율(0~1)로 채움
  - `:80~84` 화면 중앙(26px 지점)이 어두운 섹션인지 검사해 내비 색 반전
  - `:90~106` 핀 구간 진행도로 현재 장면(idx)·장면 내 진행도(local) 계산 후 전환
  - (`onScroll` `:111~115`) 스크롤 이벤트가 오면 `ticking` 깃발을 세우고 `requestAnimationFrame`으로 **다음 화면 그리기 직전 딱 1회** `update` 실행
- **왜 rAF?** 스크롤은 초당 수백 번 → 그때마다 `update`하면 낭비·버벅임. rAF는 화면 주사율(보통 60Hz)에 맞춰 **꼭 필요한 만큼만** 실행 → 부드럽고 배터리 절약.
- **연결:** 페이지의 모든 스크롤 애니메이션이 이 함수 하나로 수렴.

### 사전 16. `fcMove(dir)` — 나머지 연산으로 순환하기
- **위치:** `app.js:516`
- **한 줄 해설:** `:518` `fcState.idx = (idx + dir + n) % n`
- **원리:** `dir`은 +1(다음)/−1(이전). `% n`(나머지)이 인덱스를 0~n−1로 **감아 돌림** — 마지막(n−1) 다음은 0, 0에서 이전은 n−1. `+ n`은 음수가 나오지 않게 하는 안전장치(−1 % n 문제 방지).
- **왜 유용?** if로 "끝이면 처음으로" 분기하지 않고 **수식 한 줄**로 순환. 카드·슬라이드·캐러셀에 널리 쓰이는 패턴.
- **비유:** 시계의 12 다음이 1로 돌아가는 것과 같음(12시간 나머지 연산).

---

## 13장. 면접 대비

> 예상 질문이 나오면 위 장의 **줄번호를 짚으며** 답하세요.

**Q1. 이 서비스가 전체적으로 어떻게 돌아가나요?**
→ 브라우저(프론트)·서버(백엔드)·데이터베이스 세 덩어리로 나뉩니다(1-1). 사용자가 버튼을 누르면 `api()`(`app.js:18`)가 서버에 요청하고, 서버의 문지기 `current_user`(`app.py:84`)가 로그인을 검문한 뒤 알맞은 함수가 DB(`database.py`)를 다뤄 결과를 JSON으로 돌려주고, 브라우저가 화면을 그립니다(1-2). 오래 걸리는 작업은 진동벨 방식(백그라운드+폴링, 1-5)으로 처리합니다.

**Q2. 비밀번호는 어떻게 안전하게 저장하나요?**
→ 평문 저장 안 합니다. 사람마다 다른 **소금**을 섞어 **pbkdf2 20만 번** 해시로 저장합니다(`auth.py:67`). 해시는 되돌릴 수 없고, 소금이 레인보우 테이블을, 반복이 무차별 공격을 막습니다. 대조는 타이밍 공격을 막는 상수시간 비교(`auth.py:81`)입니다.

**Q3. 로그인 상태는 어떻게 유지되나요?**
→ 로그인 성공 시 임의 세션 토큰을 만들어(`auth.py:148`) DB에 저장하고 **httponly 쿠키**로 줍니다(`app.py:174`). 이후 요청마다 쿠키가 따라오고 서버가 주인을 확인합니다. httponly라 자바스크립트가 못 읽어 토큰 탈취를 막습니다.

**Q4. 무거운 AI 작업 중에 화면이 왜 안 멈추나요?**
→ 서버가 작업을 **백그라운드 스레드**에 맡기고 `job_id`만 즉시 반환합니다(`app.py:359`). 브라우저는 그 id로 **0.6초마다 폴링**해 진행바를 그립니다(`app.js:1827`). 진동벨 구조입니다.

**Q5. AI가 실패하거나 비용이 초과되면요?**
→ 3중 폴백입니다. ①온디바이스 → ②서버 Gemini(하루 한도) → ③규칙 기반 문자열 비교(`ai_quiz.py:358`). 어떤 상황에서도 서비스가 멈추지 않습니다.

**Q6. 온디바이스 AI가 뭐가 특별한가요?**
→ 1.4GB 모델을 브라우저에 **한 번만 다운로드(싱글톤)** 해(`ai_grade.js:63`) 채점을 **기기 안에서** 합니다. 답안이 인터넷으로 안 나가 비공개, 무료·무제한입니다. 공정성을 위해 채점은 **temperature 0**으로 항상 같은 결과를 냅니다(`ai_grade.js:167`).

**Q7. 다크모드는 어떻게 구현했나요?**
→ JS가 색을 하나씩 바꾸지 않고 최상단 태그의 **`data-theme` 한 글자만** 바꿉니다(`app.js:1897`). CSS 변수들이 그걸 보고 화면 전체 색을 한꺼번에 교체합니다. 선택값은 **localStorage**에 저장돼 유지되고 첫 방문 땐 OS 설정을 따릅니다(`app.js:1891`).

**Q8. 스크롤 애니메이션이 왜 안 버벅이나요?**
→ 스크롤은 초당 수백 번인데 **requestAnimationFrame**으로 화면 갱신 직전 딱 한 번만 계산합니다(`script.js:111`). 등장 효과는 **IntersectionObserver**로 "보일 때만" 처리하고 끝나면 감시를 끕니다(`script.js:18`).

**Q9. 데이터가 회원끼리 안 섞이나요?**
→ 모든 표에 **user_id 외래키**가 있고(`database.py`), 거의 모든 API가 `Depends(current_user)`로 본인 소유만 접근하게 검문합니다(`app.py:84`). SQL은 `?` 자리표시자로 인젝션을 막습니다(`database.py:347`).

**Q10. 왜 PDF에 폰트를 따로 등록했나요?**
→ PDF 라이브러리는 기본적으로 한글을 몰라 네모로 깨집니다. 그래서 나눔스퀘어라운드 TTF를 등록하고(`pdf_export.py:35`), 자동 줄바꿈도 없어 픽셀 폭을 재가며 직접 줄을 끊습니다(`pdf_export.py:56`).

**Q11. AI가 이상하게 답하면(지저분한 JSON) 어떻게 처리하나요?**
→ 서버는 코드펜스를 제거하고 파싱하고(`ai_quiz.py:113`), 브라우저는 **괄호 짝을 세는 파서**로 진짜 JSON 배열만 정확히 잘라냅니다(`ai_grade.js:120`). 작은 모델일수록 이런 방어가 중요합니다.

**Q12. 코드에서 가장 자랑스러운 설계는?**
→ **하이브리드 AI 채점**입니다. 브라우저에서 온디바이스로 채점해 개인정보를 지키고 비용을 없애되(`submitExam` `app.js:1579`), 실패하면 서버 Gemini로, 그것도 안 되면 규칙 기반으로 매끄럽게 폴백합니다(`grade` `ai_quiz.py:292`). 성능·비용·개인정보·안정성을 한 흐름에 담았습니다.

---

## 부록. 파일 지도와 용어집

### 파일 지도

```
VoQuiz/
├── app.py            ← ② 서버 심장. 모든 /api 창구 (920줄)
├── auth.py           ← 로그인·비밀번호 해시·세션·이메일 인증 (260줄)
├── database.py       ← ③ SQLite 표 정의 + 접근 도구 (380줄)
├── progress.py       ← 진행바용 작업 상태 저장(메모리) (92줄)
├── ai_extract.py     ← PDF/사진 → 단어 추출 (OCR+Gemini, 페이지 병렬) (244줄)
├── ai_quiz.py        ← 문제 생성 + 채점 (핵심, 3중 폴백) (392줄)
├── pdf_export.py     ← 시험지·리포트 PDF 생성 (한글 폰트) (356줄)
├── data/vocashot.db  ← 실제 데이터가 담긴 SQLite 파일
├── uploads/          ← 업로드된 PDF·생성된 PDF 저장 폴더
└── static/
    ├── script.js         ← ① 랜딩 스크롤 애니메이션 (rAF·Observer) (137줄)
    └── app/
        ├── app.js        ← ① 앱 두뇌: api·폴링·타이머·다크모드 (2465줄)
        └── ai_grade.js   ← ① 온디바이스 AI (WebGPU·싱글톤·채점) (203줄)
```

### DB 표(테이블) 지도

| 표 이름 | 무엇을 담나 | 연결 |
|---|---|---|
| `users` | 회원(이메일·비번해시·닉네임·등급·언어설정) | 모든 표의 부모 |
| `verification_codes` | 이메일 인증 코드(30분 만료) | 이메일당 1개 |
| `sessions` | 로그인 세션 토큰 | → users |
| `wordbooks` | 단어장(이름·설명·원본파일·언어) | → users |
| `words` | 단어(단어·발음·뜻·정의·예문·페이지) | → wordbooks |
| `exams` | 시험지(형식·문제 JSON) | → users, wordbooks |
| `attempts` | 응시 기록(점수·정오표·채점방식) | → exams, users |
| `report_shares` | 학부모 공유 토큰 | → users |
| `grade_reports` | "채점 이상해요" 신고(연구용) | → users |
| `pilot_devices` | 기기 WebGPU·모델로딩 계측(연구용) | → users |

### 최종 한 문장 요약

> 브라우저(`app.js`/`ai_grade.js`)가 서버(`app.py`)에 요청하면, 서버가 AI(`ai_quiz.py`/`ai_extract.py`)와 창고(`database.py`)를 부려 일을 처리하고, 오래 걸리는 일은 진동벨(`progress.py` + `pollProgress`)로 진행 상황을 보여주며, 화면은 다크모드와 부드러운 스크롤 애니메이션(`script.js`)으로 꾸며집니다.

---

*이 문서는 VoQuiz 실제 소스코드를 기준으로 작성되었습니다. 코드가 바뀌면 줄번호도 달라질 수 있으니, 발표 전 한 번 대조해 보세요.*
