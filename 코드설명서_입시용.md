# VoQuiz 코드 완전 정복 설명서 (입시 면접용)

> 이 문서 하나만 읽어도 "이 함수는 어디에 있고, 무슨 일을 하고, 전체에서 왜 필요한지"를 사정관에게 설명할 수 있도록 만들었습니다.
> 각 항목은 **파일명 → 줄 번호 → 하는 일 → 전체에서의 역할 → 쉬운 비유** 순서입니다.
> HTML/CSS 디자인 코드는 뺐고, **자바스크립트 동작(랜딩 스크롤 애니메이션·다크모드·온디바이스 AI)** 은 포함했습니다.

---

## 0. 먼저 큰 그림부터 (30초 요약)

VoQuiz는 **단어장 PDF를 찍어 올리면 → AI가 단어를 뽑고 → 시험지를 만들고 → 학생이 풀면 AI가 채점**해 주는 웹사이트입니다.

컴퓨터가 3덩어리로 나뉘어 일합니다.

| 덩어리 | 정체 | 맡은 일 | 대표 파일 |
|---|---|---|---|
| **① 브라우저(프론트엔드)** | 사용자 화면. 자바스크립트 | 버튼 클릭, 화면 그리기, 진행바, 다크모드, 온디바이스 AI 채점 | `static/app/app.js`, `ai_grade.js`, `static/script.js` |
| **② 서버(백엔드)** | 파이썬 FastAPI 프로그램 | 로그인 확인, 파일 저장, AI 호출, DB 저장 | `app.py`, `auth.py`, `ai_quiz.py`, `ai_extract.py` |
| **③ 데이터베이스** | SQLite 파일 하나 | 회원·단어·시험지·점수를 영구 보관 | `database.py`, `data/vocashot.db` |

**비유:** 브라우저는 *손님이 보는 식당 홀*, 서버는 *주방*, 데이터베이스는 *창고*입니다. 손님(브라우저)이 주문(요청)을 넣으면 주방(서버)이 창고(DB)에서 재료를 꺼내 요리해 돌려줍니다. 이 "주문↔응답"을 **API** 라고 부릅니다.

또 하나 중요한 특징: AI 채점이 **두 갈래**입니다.
- **온디바이스 AI**: 내 컴퓨터(브라우저) 안에서 직접 돌아가는 AI (무료·무제한·비공개). → `ai_grade.js`
- **서버 Gemini**: 구글의 클라우드 AI (성능 좋지만 유료라 하루 한도). → `ai_quiz.py`
- 온디바이스가 준비돼 있으면 그걸 쓰고, 안 되면 자동으로 Gemini로 넘어갑니다. 이걸 **폴백(fallback, 대체 작동)** 이라고 합니다.

---

## 1. 서버의 심장 — `app.py` (API 서버, 920줄)

이 파일은 **"주방장"** 입니다. 브라우저가 보내는 모든 주문(`/api/...`)을 받아서 알맞은 부서(auth, ai_quiz 등)에 넘기고 결과를 돌려줍니다. `@app.post(...)`, `@app.get(...)` 이라고 붙은 함수 하나하나가 "주문 창구 한 개"입니다.

### 1-1. `current_user()` — 로그인 검문소
- **위치:** `app.py:84`
- **하는 일:** 브라우저가 보낸 쿠키에서 `session` 토큰을 꺼내(`app.py:86`), 그게 진짜 로그인된 사람인지 `auth`에 물어봅니다(`app.py:87`). 아니면 즉시 "401 로그인 필요" 오류를 던집니다.
- **전체 역할:** 거의 모든 API 함수가 `user=Depends(current_user)` 라고 적어두는데, 그러면 FastAPI가 **그 함수를 실행하기 전에 자동으로 이 검문을 먼저 통과시킵니다.** 즉 내 단어장·시험지에 남이 못 들어오게 막는 **문지기**입니다.
- **비유:** 놀이공원 입구에서 팔찌를 확인하는 직원. 팔찌 없으면 못 들어감.

### 1-2. `signup()` / `verify()` / `login()` — 회원가입 3단계
- **위치:** 가입 `app.py:112`, 인증 `app.py:140`, 로그인 `app.py:165`
- **하는 일 (순서대로):**
  1. `signup`: 가입 키 확인(`:114`) → 이메일·비밀번호 형식 검사(`:117`, `:119`) → 아직 인증 안 된 상태로 계정 생성 후 **6자리 인증 코드**를 이메일로 발송(`:130`).
  2. `verify`: 사용자가 입력한 코드가 맞는지 확인(`:143`) → 맞으면 "인증 완료" 도장을 찍고(`:145`) → **세션 쿠키**를 발급해 바로 로그인 상태로 만듦(`:147~148`).
  3. `login`: 이메일·비밀번호를 대조(`:169`)하고, 이메일 인증까지 된 사람만 통과시켜(`:171`) 세션 쿠키를 줍니다.
- **전체 역할:** "진짜 이메일 주인만 가입"하도록 막는 절차. 스팸·가짜 계정 방지.
- **비유:** 새 회원카드를 만들 때 문자로 온 인증번호를 입력해야 카드가 활성화되는 것과 같습니다.

### 1-3. `upload_file()` — 파일 받기
- **위치:** `app.py:336`
- **하는 일:** PDF나 이미지 파일을 받아(`:337`) 확장자를 확인하고(`:339`), `u3_1783822090.pdf` 처럼 **누가·언제 올렸는지 알 수 있는 안전한 이름**으로 바꿔 서버 폴더에 저장합니다(`:342~345`). PDF면 총 페이지 수를 세서 돌려줍니다(`:346`).
- **전체 역할:** 단어 추출의 첫 단추. 여기서 저장한 파일을 다음 단계(추출)가 읽어갑니다.
- **비유:** 사진관에 필름을 맡기면 접수번호를 붙여 보관하는 것.

### 1-4. `extract_start()` — 단어 추출을 "백그라운드"로 시작 ⭐
- **위치:** `app.py:359`
- **하는 일 (핵심 개념):** PDF에서 단어를 뽑는 건 몇십 초가 걸립니다. 그동안 화면이 멈추면 안 되니까, 이 함수는 **작업을 바로 끝내지 않고**:
  1. `progress.create_job()` 으로 "작업표(job_id)"를 하나 만들고(`:366`),
  2. 실제 무거운 추출 작업은 **별도의 일꾼 스레드**에게 맡긴 뒤(`worker` 함수 `:371`, 스레드 시작 `:382`),
  3. 브라우저에는 **job_id만 즉시** 돌려줍니다(`:383`).
- **전체 역할:** 브라우저는 이 job_id로 "얼마나 됐어?"를 반복해서 물어보며(→ 폴링) 진행바를 그립니다. 문제 생성(`generate_start` `:494`), 채점(`grade_start` `:605`)도 **똑같은 구조**입니다.
- **비유:** 식당에서 오래 걸리는 요리를 주문하면 **진동벨**을 주고 손님을 자리로 보냅니다. 손님은 벨(job_id)을 들고 "다 됐나?"를 확인하죠. 손님을 계산대에 계속 세워두지 않는 게 핵심입니다.
- **왜 스레드?** 파이썬 서버는 여러 손님을 동시에 상대해야 하는데, 한 명의 추출이 끝날 때까지 서버 전체가 멈추면 다른 손님이 다 기다려야 합니다. 스레드는 "일꾼 한 명을 따로 붙여" 그 손님만 처리하게 하는 방법입니다.

### 1-5. `save_wordbook()` — 단어장 저장
- **위치:** `app.py:395`
- **하는 일:** 추출된 단어들을 DB의 `wordbooks`(단어장) 테이블에 한 줄, `words`(단어) 테이블에 여러 줄 저장합니다(`:405`, `:416`). `executemany`로 단어 수십 개를 한 번에 넣습니다(`:416`).
- **전체 역할:** 임시로 떠 있던 추출 결과를 "영구 보관"으로 바꾸는 단계.

### 1-6. `grade_start()` — 채점 시작 (온디바이스 vs 서버 분기) ⭐
- **위치:** `app.py:605`
- **하는 일:** 시험지와 학생 답안을 받아 `ai_quiz.grade(...)` 에 채점을 맡깁니다(`:621`). 여기서 **브라우저가 미리 온디바이스로 채점한 결과(`ai_pregraded`)를 함께 받는 것**이 포인트입니다. 채점이 끝나면 결과를 `attempts`(응시기록) 테이블에 저장(`:622`)하고, 채점 방식(`graded_by`: ondevice/server)도 같이 기록합니다.
- **전체 역할:** 시스템에서 가장 중요한 "AI 채점"의 서버 쪽 입구. 응시 기록이 여기서 영구 저장되어 대시보드·리포트의 점수가 됩니다.

### 1-7. `get_stats()` — 대시보드 통계 집계
- **위치:** `app.py:276`
- **하는 일:** 내 단어장 수·단어 수·시험지 수·평균/최고 점수·이번 주 응시 수·최근 기록을 **SQL 집계 쿼리**(`COUNT`, `AVG`, `MAX`)로 한꺼번에 계산해 돌려줍니다(`:280~303`).
- **전체 역할:** 대시보드 화면의 숫자·차트가 전부 이 함수 하나에서 나옵니다.
- **비유:** 성적표를 만들 때 시험지들을 다 훑어 평균·최고점을 계산하는 것.

### 1-8. 학부모 공유 리포트 — `create_share()` / `public_report()`
- **위치:** 토큰 발급 `app.py:687`, 공개 열람 `app.py:713`
- **하는 일:** `secrets.token_urlsafe(12)` 로 **추측 불가능한 임의 문자열(토큰)** 을 만들어(`:694`) 링크에 넣습니다. 부모님은 로그인 없이 그 링크만으로 자녀 진도를 볼 수 있습니다(`public_report`). 단, 그 토큰이 DB에 있는지 확인해서(`:716`) 아무 링크나 통하지 않게 막습니다.
- **전체 역할:** "로그인은 시키기 싫지만 특정 사람에게만 보여주고 싶다"를 해결하는 방법. 토큰 자체가 열쇠입니다.
- **비유:** 구글 문서의 "링크 아는 사람만 보기". 링크가 곧 비밀번호.

### 1-9. `_no_cache_static()` — 옛날 화면 캐시 방지
- **위치:** `app.py:57`
- **하는 일:** HTML/JS/CSS 파일에 "매번 새로 확인해"라는 헤더를 붙입니다(`:63`).
- **전체 역할:** 코드를 새로 배포했는데 브라우저가 옛날 `app.js`를 기억(캐시)해서 쓰는 버그를 막습니다.
- **비유:** 게시판 공지를 바꿨는데 사람들이 옛날 사진(스크린샷)만 보는 걸 막으려고 "항상 최신판 보세요" 도장을 찍는 것.

### 1-10. 맨 마지막 줄 — 정적 파일 연결
- **위치:** `app.py:920` — `app.mount("/", StaticFiles(...))`
- **하는 일:** `/api/...` 가 아닌 모든 주소는 `static/` 폴더의 실제 파일(HTML/JS/이미지)을 그대로 내보냅니다.
- **전체 역할:** 같은 서버 하나가 **API도, 웹사이트 화면도** 둘 다 서빙합니다. 그래서 프론트엔드가 API를 부를 때 주소 없이 `/api/...` 상대경로만 써도 됩니다.

---

## 2. 로그인·보안 담당 — `auth.py` (260줄)

이 파일은 **"경비실"** 입니다. 비밀번호를 안전하게 보관하고, 세션과 이메일 인증을 관리합니다. 입시 면접에서 **보안 개념**을 물어보기 딱 좋은 부분입니다.

### 2-1. `hash_password()` — 비밀번호를 "되돌릴 수 없게" 저장 ⭐⭐
- **위치:** `auth.py:67`
- **하는 일:** 비밀번호를 그대로 저장하지 않습니다. 대신:
  1. `salt`(소금) = 사용자마다 다른 랜덤 문자열을 만들고(`:69`),
  2. 비밀번호+소금을 **pbkdf2 해시 함수로 20만 번 반복**해 뒤섞어(`:70`),
  3. `소금$해시` 형태로 저장합니다(`:71`).
- **전체 역할:** 만약 해커가 DB를 훔쳐도 **원래 비밀번호를 알 수 없습니다.** 해시는 한 방향(비밀번호→해시)만 되고 반대(해시→비밀번호)는 안 되기 때문입니다.
- **왜 소금(salt)?** 소금이 없으면 "1234"라는 비밀번호는 항상 똑같은 해시가 됩니다. 그럼 해커가 미리 만든 표(레인보우 테이블)로 뚫립니다. 사람마다 다른 소금을 섞으면 같은 비밀번호라도 결과가 전부 달라져 그 표가 무용지물이 됩니다.
- **왜 20만 번?** 일부러 느리게 만들어, 해커가 수억 개를 대입(무차별 공격)하는 걸 어렵게 합니다.
- **비유:** 소금은 "사람마다 다른 양념", 해시는 "믹서기에 갈기". 스무디를 다시 원래 과일로 되돌릴 수 없듯, 비밀번호도 되돌릴 수 없습니다.

### 2-2. `verify_password()` — 로그인 시 대조
- **위치:** `auth.py:74`
- **하는 일:** 입력한 비밀번호를 **같은 소금으로 똑같이 해시**해서, 저장된 해시와 비교합니다(`:80`). 이때 `hmac.compare_digest`(`:81`)로 **상수 시간 비교**를 합니다.
- **상수 시간 비교란?** 보통 문자열 비교는 앞 글자가 틀리면 바로 멈춥니다. 그 "멈추는 시간 차이"를 해커가 재서 비밀번호를 한 글자씩 알아낼 수 있는데(타이밍 공격), `compare_digest`는 항상 같은 시간이 걸려 이걸 막습니다.
- **비유:** 정답과 비교할 때, 틀린 순간 티 내지 않고 끝까지 똑같은 표정으로 확인하는 것.

### 2-3. `validate_password()` — 비밀번호 규칙 검사
- **위치:** `auth.py:45`
- **하는 일:** 6~15자, 영문·숫자·특수문자 각각 1개 이상인지 **정규식(re.search)** 으로 확인합니다(`:53~60`).
- **전체 역할:** 너무 쉬운 비밀번호를 애초에 막습니다. 서버(`app.py:119`)와 브라우저(`app.js` `checkPwRules`) **양쪽에서** 검사하는데, 브라우저 검사는 편의(빠른 안내)용이고 **서버 검사가 진짜 방어선**입니다. (해커는 브라우저 검사를 건너뛸 수 있으니까요.)

### 2-4. `issue_verification_code()` / `check_verification_code()` — 이메일 인증 코드
- **위치:** 발급 `auth.py:87`, 확인 `auth.py:100`
- **하는 일:** `secrets.randbelow(1000000)` 으로 6자리 난수 코드를 만들고(`:89`), **30분 유효시간**을 붙여 저장합니다(`:90`). 확인할 때는 만료 여부(`:105`)와 코드 일치(`:107`)를 보고, 맞으면 재사용 못 하게 즉시 삭제합니다(`:109`).
- **전체 역할:** "이 이메일이 진짜 네 것"임을 증명하게 합니다.

### 2-5. `send_verification_email()` — 실제/개발 모드 이메일 발송
- **위치:** `auth.py:113`
- **하는 일:** `.env`에 SMTP(메일 서버) 설정이 있으면 진짜 이메일을 보내고(`:138~142`), 없으면 **개발 모드**로 판단해 코드를 서버 화면(콘솔)에 출력만 합니다(`:124`).
- **전체 역할:** 개발할 때는 실제 메일 없이 테스트하고, 운영할 때는 진짜 발송하도록 **한 코드로 두 상황**을 처리합니다.
- **비유:** 연습 경기(개발)에서는 심판이 "골!"이라고 말로만 하고, 실제 경기(운영)에서는 전광판에 띄우는 것.

### 2-6. `create_session()` / `get_user_by_token()` — 세션(로그인 유지) ⭐
- **위치:** 생성 `auth.py:148`, 조회 `auth.py:159`
- **하는 일:** 로그인에 성공하면 `secrets.token_urlsafe(32)` 로 긴 임의 토큰을 만들어(`:150`) DB `sessions` 표에 저장하고, 그 토큰을 **쿠키**로 브라우저에 심습니다(app.py `:148`, `:174`). 다음부터 브라우저는 요청마다 이 쿠키를 자동으로 들고 오고, 서버는 `get_user_by_token`으로 "이 토큰의 주인이 누구지?"를 확인합니다.
- **전체 역할:** 페이지를 옮길 때마다 비밀번호를 다시 안 물어봐도 되게 하는 **"로그인 유지"의 핵심**.
- **왜 비밀번호 대신 토큰?** 비밀번호를 매번 주고받으면 위험하니까, 로그인은 한 번만 하고 그 다음엔 "임시 팔찌(토큰)"로 통과시킵니다. 로그아웃하면 이 팔찌를 버립니다(`delete_session` `:170`).
- **쿠키에 붙은 보안 옵션(app.py `:148`):** `httponly`(자바스크립트가 못 읽음 → 해킹 스크립트로 토큰 탈취 방지), `samesite=lax`(다른 사이트에서 몰래 요청하는 CSRF 공격 완화), `secure`(HTTPS에서만 전송).

---

## 3. 데이터 창고 — `database.py` (380줄)

이 파일은 **"창고 관리인"** 입니다. 모든 정보를 SQLite 파일 하나(`data/vocashot.db`)에 표(테이블) 형태로 정리해 보관합니다.

### 3-1. 왜 "관계형" 데이터베이스인가
- **위치:** 스키마 정의 `database.py:53~275`
- **핵심 아이디어:** 정보를 여러 표로 나누고 **번호(id)로 연결**합니다.
  - `users`(회원) — `wordbooks`(단어장) — `words`(단어) — `exams`(시험지) — `attempts`(응시기록)
  - 예: 단어(`words`)마다 `wordbook_id`가 붙어 "몇 번 단어장에 속하는지"를 가리킵니다(`:99`). 이걸 **외래키(FK)** 라고 합니다.
- **전체 역할:** 회원끼리 데이터가 안 섞이고(각 표에 `user_id`), "한 단어장에 단어 여러 개"(1:N 관계)가 자연스럽게 표현됩니다.
- **비유:** 학교에서 학생마다 학번을 주고, 성적표·출석부·사물함을 전부 학번으로 연결하는 것. 이름이 같은 학생이 있어도 학번으로 구분됩니다.

### 3-2. `ON DELETE CASCADE` — 연쇄 삭제
- **위치:** 예 `database.py:86`, `:99`
- **하는 일:** 회원을 지우면 그 회원의 단어장·단어·시험지·기록이 **자동으로 함께** 지워집니다.
- **전체 역할:** 주인 없는 고아 데이터가 남지 않게 합니다.
- **비유:** 사물함을 없애면 그 안의 물건도 같이 치워지는 것.

### 3-3. `execute()` / `query_one()` / `query_all()` — DB 접근 도구
- **위치:** `database.py:347`, `:361`, `:368`
- **하는 일:** SQL 명령을 실행하는 3개의 헬퍼. `execute`는 저장/삭제(commit), `query_one`은 한 줄 읽기, `query_all`은 여러 줄 읽기입니다. 결과를 파이썬 딕셔너리(`dict`)로 바꿔 다루기 쉽게 합니다(`:365`).
- **매개변수 `?` 의 중요성(보안):** SQL을 만들 때 값을 직접 문자열로 붙이지 않고 `?` 자리표시자에 따로 넣습니다(예 `app.py:280`). 이것이 **SQL 인젝션**(악의적 입력으로 DB를 조작하는 해킹)을 막는 표준 방법입니다.
- **비유:** 편지 양식의 빈칸에 값을 "따로" 채워 넣는 것. 값이 명령어처럼 취급되지 않습니다.

### 3-4. `_write_lock` — 동시 저장 충돌 방지
- **위치:** 정의 `database.py:28`, 사용 `:350`, `:377`
- **하는 일:** 여러 작업이 동시에 DB에 쓰려 할 때, **한 번에 하나씩만** 쓰도록 자물쇠를 겁니다.
- **전체 역할:** 1-4에서 본 스레드 여러 개가 동시에 돌 때 데이터가 깨지지 않게 합니다.
- **비유:** 화장실 문 잠금. 한 명이 쓰는 동안 다른 사람은 밖에서 기다립니다.

### 3-5. `_migrate()` — 표에 새 칸(컬럼) 추가하기
- **위치:** `database.py:286`
- **하는 일:** 앱을 업데이트하며 회원 표에 `nickname`, `avatar`, `explain_lang` 같은 새 칸이 필요해졌을 때, **기존 데이터를 지우지 않고** 없는 칸만 살며시 추가합니다(`:296~342`).
- **전체 역할:** 이미 가입한 회원 정보를 보존하면서 기능을 늘리는 **"데이터베이스 마이그레이션"**.
- **비유:** 이미 다 쓴 공책에 새 항목 칸을 뒤늦게 그려 넣되, 기존에 쓴 내용은 그대로 두는 것.

---

## 4. 진행바 엔진 — `progress.py` (92줄)

이 작은 파일이 "진동벨" 시스템 전체를 담당합니다.

### 4-1. `_jobs` 딕셔너리 (인메모리 저장소)
- **위치:** `progress.py:19`
- **하는 일:** 진행 중인 작업들을 `{job_id: {진행률, 메시지, 로그, 결과...}}` 형태로 **컴퓨터 메모리에** 담아둡니다.
- **전체 역할:** 무거운 작업(추출·생성·채점)의 상태를 여기 계속 갱신하면, 브라우저가 이걸 읽어 진행바를 그립니다.
- **비유:** 주방 벽에 붙은 화이트보드. 요리 상태("50% 완료")를 계속 지웠다 다시 씁니다.

### 4-2. `create_job()` / `update()` / `finish()` / `fail()` / `get()`
- **위치:** `progress.py:23`, `:39`, `:56`, `:68`, `:79`
- **하는 일:** 작업 생성(고유 id 발급 `:24`), 진행률·로그 갱신(`:39`), 성공 마감(`:56`), 실패 기록(`:68`), 현재 상태 읽기(`:79`).
- **`_lock` 사용:** 여러 스레드가 동시에 화이트보드를 고치다 엉키지 않게 자물쇠를 씁니다(`:20`, `:26` 등).
- **왜 메모리?** 진행 상태는 잠깐 쓰고 버리는 정보라 DB까지 갈 필요가 없습니다. 서버를 껐다 켜면 사라지지만 문제없습니다.

---

## 5. 단어 추출 AI — `ai_extract.py` (244줄)

**"PDF/사진을 눈으로 읽어 단어를 뽑는 부서"** 입니다. OCR(글자 인식)과 AI를 합쳤습니다.

### 5-1. `extract_words()` — 추출 총괄 + 페이지 병렬 처리 ⭐
- **위치:** `ai_extract.py:162`
- **하는 일:**
  1. PDF를 페이지별 이미지로 변환합니다(`convert_from_path` `:183`, 해상도 300dpi로 또렷하게).
  2. **여러 페이지를 동시에** AI로 보냅니다. `ThreadPoolExecutor(max_workers=5)`로 최대 5페이지를 병렬 처리(`:196`)해 속도를 크게 올립니다.
  3. 한 페이지가 끝날 때마다 진행바를 갱신합니다(`report(...)` `:208`).
  4. 페이지 순서대로 정렬하고 순번을 붙여 마무리합니다(`:223`, `_finalize` `:228`).
- **전체 역할:** "종이 단어장 → 디지털 단어 데이터"로 바꾸는 마법의 첫 단계.
- **병렬 처리 비유:** 100장 시험지를 선생님 한 명이 채점하면 오래 걸리지만, 5명이 나눠 채점하면 5배 빠릅니다. 페이지들은 서로 상관없어서 동시에 처리해도 됩니다.

### 5-2. `_gemini_extract_image()` — 이미지 한 장을 AI에게
- **위치:** `ai_extract.py:117`
- **하는 일:** 이미지를 **base64 문자열로 인코딩**(`_encode_image` `:106`)해 Gemini에 보내고, "단어 목록을 JSON으로 달라"고 요청합니다(`:126~132`). `temperature: 0.1`로 설정해(`:131`) AI가 매번 **일관되게** 답하도록 합니다.
- **base64란?** 그림 같은 이진 데이터를 글자로만 표현하는 방식. 인터넷은 글자를 주고받기 편해서, 그림을 글자로 바꿔 보냅니다.
- **비유:** 그림을 "점자처럼 글자로 번역"해서 편지에 담아 보내는 것.

### 5-3. 프롬프트(AI 지시문) — `PROMPT_EN`, `PROMPT_ZH`
- **위치:** 영어 `ai_extract.py:30`, 중국어 `:57`
- **하는 일:** AI에게 **"사전 편집자처럼 행동하라"** 고 매우 구체적으로 지시합니다. 특히 중요한 규칙(`:36~44`): 이미지에 영어 정의가 있으면 **그 정의를 한국어로 번역**하고(단어만 딸랑 번역하지 말고), 정의가 없으면 AI가 사전처럼 뜻·예문을 새로 생성하라는 것.
- **전체 역할:** AI의 출력 품질은 프롬프트가 결정합니다. "Bank"가 문맥상 '둑'인지 '은행'인지 구별하게 만드는 게 이 지시문(`:43`)입니다. → **프롬프트 엔지니어링**의 실제 예시.

### 5-4. `_finalize()` — 결과 청소
- **위치:** `ai_extract.py:228`
- **하는 일:** 단어가 비어 있는 항목을 버리고(`:232`), 공백을 정리하고(`.strip()`), 순번(`seq`)을 붙입니다.
- **전체 역할:** AI가 가끔 지저분하게 준 결과를 깔끔하게 다듬는 "품질 검사".

---

## 6. 문제 생성 & 채점 AI — `ai_quiz.py` (392줄) ⭐핵심

프로젝트에서 **가장 중요한 파일**입니다. 시험지를 만들고, 주관식을 채점합니다.

### 6-1. `generate_quiz()` — 시험 문제 만들기
- **위치:** `ai_quiz.py:131`
- **하는 일:**
  1. 단어 목록을 섞어 필요한 개수만 뽑습니다(`random.shuffle` `:145`).
  2. **주관식(fill_meaning)** 은 AI 없이 코드로 직접 만듭니다(`:159`) — 단어를 보여주고 뜻을 쓰게 하는 단순 형식이라 AI가 필요 없습니다.
  3. **객관식(토플/SAT/4지선다)** 은 Gemini에게 "이 단어들로 4지선다 문제를 만들라"고 요청합니다(프롬프트 `:190`, 호출 `:210`).
  4. AI가 준 문제를 검증·정리하고(`:214~228`), 각 문제가 **어떤 원본 단어**를 다루는지 짝지어(`_match_source_word` `:233`) 나중에 취약 단어 분석에 씁니다.
- **전체 역할:** 단어장 하나로 다양한 유형의 시험지를 자동 생성. VoQuiz의 대표 기능.

### 6-2. `grade()` — 채점 총괄 (객관식은 코드, 주관식은 AI) ⭐
- **위치:** `ai_quiz.py:292`
- **하는 일:**
  1. **객관식**은 답이 딱 정해져 있으니 코드로 정확히 비교합니다(`:308~320`) — 빠르고 공짜.
  2. **주관식**은 학생이 자기 말로 쓰므로 AI가 "뜻이 비슷하면 정답"으로 판단합니다(`:322~349`).
  3. 여기서 **분기**(`:333`): 브라우저가 미리 온디바이스로 채점한 결과(`ai_pregraded`)가 있으면 그걸 쓰고(`graded_by="ondevice"`), 없으면 서버 Gemini로 채점합니다(`graded_by="server"`).
  4. 맞은 개수로 백분율 점수를 계산합니다(`:351~352`).
- **전체 역할:** "뜻이 통하면 맞다"는 사람 같은 채점을 자동화. 이 프로젝트의 핵심 가치.
- **비유:** 객관식은 OMR 카드 채점(기계), 주관식은 선생님이 서술형 답을 읽고 "의미가 맞네" 하고 채점하는 것.

### 6-3. `_grade_written()` — 주관식 서버 채점 + 3중 안전장치 ⭐⭐
- **위치:** `ai_quiz.py:358`
- **하는 일 (폴백 구조):**
  1. 먼저 **하루 한도**를 확인합니다(`:361`). Gemini는 유료라 하루 100문제(`GEMINI_DAILY_LIMIT` `:38`)로 제한.
  2. 한도 안이면 Gemini로 채점하고(`:378`), 성공 시 사용량을 파일에 기록합니다(`:380`).
  3. **한도 초과 또는 Gemini 실패 시** → 단순 문자열 비교(정확히 같으면 정답)로 **폴백**합니다(`:384~392`).
- **전체 역할:** AI가 실패해도, 돈이 다 떨어져도 **서비스가 절대 멈추지 않게** 하는 방어 설계. "완벽하진 않아도 항상 답은 준다."
- **폴백 비유:** 엘리베이터가 고장 나면 계단으로 가는 것. 느리고 불편해도 목적지엔 도착합니다.
- **사용량 기록:** `_gemini_usage_today()`(`:43`)와 `_gemini_add_usage()`(`:56`)가 `gemini_usage.json` 파일에 "오늘 몇 문제 썼는지"를 날짜와 함께 저장. 날짜가 바뀌면 0으로 리셋됩니다.

### 6-4. `_gemini_json()` — AI 응답을 JSON으로 안전하게 파싱
- **위치:** `ai_quiz.py:99`
- **하는 일:** Gemini에 프롬프트를 보내고(`:107`), 응답에서 AI가 종종 붙이는 ` ```json ` 같은 마크다운 껍데기를 제거한 뒤(`:113`) 진짜 JSON만 뽑아 파싱합니다(`:114`).
- **전체 역할:** AI 응답은 지저분할 수 있어서, 이걸 프로그램이 쓸 수 있는 깔끔한 데이터로 바꾸는 "통역·정리" 담당.

### 6-5. `check_usage()` — 예문 첨삭 AI
- **위치:** `ai_quiz.py:257`
- **하는 일:** 학생이 플래시카드에서 직접 쓴 예문을, AI 영어 선생님이 되어 "이 단어를 문맥·문법에 맞게 썼는지" 판단하고 따뜻한 피드백을 줍니다.
- **전체 역할:** 단순 암기를 넘어 "직접 써보고 교정받는" 능동적 학습을 지원.

---

## 7. PDF 만들기 — `pdf_export.py` (356줄)

시험지·리포트를 인쇄 가능한 PDF로 그립니다.

### 7-1. `_register_fonts()` — 한글 폰트 등록
- **위치:** `pdf_export.py:27`
- **하는 일:** PDF 라이브러리는 기본적으로 한글을 모릅니다. 그래서 프로젝트에 넣어둔 나눔스퀘어라운드 TTF 폰트를 등록하고(`:35`), 명조·고딕은 내장 한중일 폰트를 씁니다(`:41~44`). 한 번만 등록하도록 캐시합니다(`_FONTS_READY` `:29`).
- **전체 역할:** PDF에서 한글이 네모(□□□)로 깨지지 않게 하는 필수 준비.

### 7-2. `_wrap_w()` — 줄바꿈 직접 계산
- **위치:** `pdf_export.py:56`
- **하는 일:** 글자가 종이 폭을 넘으면 줄을 바꿉니다. 한글은 글자 단위, 영어는 되도록 단어 단위로 끊습니다(`stringWidth`로 실제 픽셀 폭을 재가며 `:62`).
- **전체 역할:** 웹과 달리 PDF는 자동 줄바꿈이 없어서, 텍스트가 종이 밖으로 삐져나가지 않게 직접 계산해야 합니다.

### 7-3. `create_quiz_pdf()` / `create_report_pdf()` / `create_progress_pdf()`
- **위치:** `pdf_export.py:128`, `:252`, `:300`
- **하는 일:** 각각 시험지, 채점 리포트, 학습 진도 리포트를 A4 캔버스에 좌표를 지정해가며 그립니다(`canvas` 사용).
- **전체 역할:** 선생님이 인쇄해서 나눠주거나 부모님께 보여줄 수 있는 종이 산출물 생성.

---

## 8. 프론트엔드 두뇌 — `static/app/app.js` (2465줄)

브라우저에서 도는 자바스크립트. 화면 전환·버튼·진행바·다크모드를 모두 담당합니다.

### 8-1. `api()` — 서버와 대화하는 단 하나의 통로 ⭐
- **위치:** `app.js:18`
- **하는 일:** 모든 서버 요청이 이 함수를 거칩니다. `fetch`로 요청을 보내고(`:26`), **`credentials: "include"`** 로 세션 쿠키를 꼭 함께 보냅니다(`:19`). 응답을 JSON으로 바꾸고, 실패하면 오류 메시지를 던집니다(`:29`).
- **전체 역할:** 브라우저↔서버 통신을 한 곳에 모아, 쿠키·오류 처리를 매번 반복하지 않게 합니다.
- **`async/await`란?** 서버 응답은 시간이 걸립니다. `await`는 "응답 올 때까지 기다렸다가 다음 줄 실행"이라는 뜻인데, **화면은 안 멈춥니다.** 다른 일을 하다가 응답이 오면 이어서 처리합니다.
- **비유:** 친구에게 카톡을 보내고(요청), 답이 올 때까지 멍하니 폰만 보는 게 아니라 다른 일 하다가 답 오면(await) 읽는 것.

### 8-2. `pollProgress()` — 진동벨을 계속 확인 ⭐
- **위치:** `app.js:1827`
- **하는 일:** 서버가 준 `job_id`로 **0.6초마다** `/api/progress/{job_id}`를 반복 요청합니다(`while(true)` + `sleep(600)` `:1829~1830`). 받은 진행률·로그로 진행바를 갱신하고(`:1834~1838`), 상태가 `done`이면 결과를 돌려주고(`:1839`) `error`면 오류를 던집니다(`:1840`).
- **전체 역할:** 1-4의 백그라운드 작업과 짝을 이루는 브라우저 쪽 절반. **폴링(polling)** = "다 됐어? 다 됐어?" 반복해서 물어보기.
- **비유:** 진동벨을 들고 "울렸나?" 계속 확인하는 것. 울리면(done) 음식을 받으러 갑니다.
- **`showProgress()`(`:1798`):** 진행바 화면 요소(막대·퍼센트·로그)를 조작하는 도구 묶음을 만들어 `pollProgress`에 넘깁니다.

### 8-3. `submitExam()` — 시험 제출 & 온디바이스 채점 연결 ⭐
- **위치:** `app.js:1579`
- **하는 일:**
  1. 타이머를 멈추고(`:1580`), 주관식 답안만 모읍니다(`:1586~1591`).
  2. **온디바이스 AI가 준비돼 있으면** 브라우저에서 먼저 채점합니다(`gradeWrittenOnDevice` 호출 `:1597`).
  3. 그 결과(`ai_pregraded`)를 서버로 함께 보냅니다(`:1602`).
  4. `pollProgress`로 채점 완료를 기다렸다가 리포트를 그립니다(`:1604~1606`).
- **전체 역할:** "브라우저 AI 채점 → 서버 저장"이라는 하이브리드 흐름의 지휘자. 온디바이스가 없으면 이 단계를 건너뛰고 서버(Gemini)가 채점합니다.

### 8-4. 타이머 — `startTimer()` / `stopTimer()`
- **위치:** `app.js:1556`, `:1573`
- **하는 일:** `setInterval`로 **1초마다** 시간을 1씩 올립니다(`:1558`). 제한시간 모드면 남은 시간을 계산해 0이 되면 자동 제출하고(`:1564`), 30초 이하면 빨간 경고를 켭니다(`:1566`).
- **`setInterval`이란?** "이 코드를 N밀리초마다 반복 실행해줘"라는 명령. `clearInterval`로 멈춥니다(`stopTimer` `:1573`).
- **왜 멈춰야 하나?** 화면을 벗어나도 타이머가 계속 돌면 메모리를 낭비하고 오작동합니다. 그래서 화면 전환 때마다 정리합니다(`showView` `:42`).
- **비유:** 스톱워치. 켜두면 계속 흐르니, 다 쓰면 꼭 꺼야 합니다.

### 8-5. 다크모드 — `initTheme()` / `applyTheme()` / `toggleTheme()` ⭐
- **위치:** `app.js:1891`, `:1897`, `:1907`
- **하는 일:**
  1. `initTheme`(`:1891`): 페이지가 열리면 **저장된 테마 설정**(`localStorage`)을 먼저 확인하고(`:1893`), 없으면 **운영체제 설정**(`prefers-color-scheme`)을 따릅니다(`:1894`).
  2. `applyTheme`(`:1897`): 문서 최상단 태그에 `data-theme="dark"` 같은 표시를 붙입니다(`:1898`). **CSS는 이 표시를 보고 색을 통째로 바꿉니다.** 동시에 토글 버튼 아이콘을 해/달로 바꾸고(`:1900`), 로고 이미지도 밝은판/어두운판으로 교체합니다(`:1904`).
  3. `toggleTheme`(`:1907`): 지금이 다크면 라이트로, 라이트면 다크로 뒤집고(`:1908`) **localStorage에 저장**해(`:1909`) 다음 방문에도 유지합니다.
- **전체 역할:** 자바스크립트는 색을 하나하나 바꾸지 않습니다. **딱 한 글자(`data-theme`)만 바꾸면**, 미리 준비된 CSS 변수들이 화면 전체 색을 한꺼번에 갈아끼웁니다. 아주 효율적인 설계입니다.
- **`localStorage`란?** 브라우저에 작은 메모를 영구 저장하는 곳. 탭을 닫아도 남습니다. 그래서 다음에 와도 다크모드가 기억됩니다.
- **비유:** 방 전체 조명을 전구마다 바꾸는 게 아니라, **메인 스위치 하나**를 '밤 모드'로 돌리면 방 안 모든 조명이 한꺼번에 바뀌는 것. 그 스위치 위치를 메모지(localStorage)에 적어둬 다음에도 그대로 켭니다.

### 8-6. 아이콘 시스템 — `ICONS` / `applyIcons()`
- **위치:** 정의 `app.js:1849`, 적용 `:1863`
- **하는 일:** 아이콘들을 **SVG(그림을 그리는 코드)** 문자열로 저장하고, `stroke="currentColor"`(`:1848`)를 써서 **글자색을 따라가게** 합니다. 그래서 다크모드로 바뀌면 아이콘도 자동으로 흰색이 됩니다.
- **전체 역할:** 이미지 파일 없이 코드로 아이콘을 그려, 어떤 배경·테마에서도 선명하고 색이 자동으로 맞습니다.

### 8-7. `mdToHtml()` — 간단 마크다운 변환 + XSS 방지
- **위치:** `app.js:2044`
- **하는 일:** AI 튜터 답변의 `**굵게**`, `- 목록` 같은 마크다운을 HTML로 바꿉니다. 중요한 건 **먼저 `esc()`로 위험한 문자를 무력화**한 뒤(`:2045`) 변환한다는 점입니다.
- **XSS(교차 사이트 스크립팅)란?** 누군가 답변에 악성 스크립트를 심으면 그게 실행돼 해킹될 수 있습니다. `esc()`(`:2419`)가 `<` 같은 문자를 무해한 기호로 바꿔 이걸 막습니다. 사용자 입력은 항상 `textContent`로 그대로 넣어(`:2037`) 실행되지 않게 합니다.
- **비유:** 손님이 준 쪽지를 그대로 읽지 않고, 명령어처럼 보이는 부분을 다 "따옴표 처리"해서 안전하게 보여주는 것.

### 8-8. `sendChat()` — AI 튜터 대화
- **위치:** `app.js:2062`
- **하는 일:** 온디바이스 AI가 준비됐는지 확인하고(`:2066`), 최근 대화 8개만 맥락으로 보내(`:2075`) 브라우저 안의 AI(`chatOnDevice`)에게 답을 받습니다. **단어 학습 질문만** 답하도록 제한돼 있습니다(시스템 프롬프트, ai_grade.js `:184`).

---

## 9. 온디바이스 AI — `static/app/ai_grade.js` (203줄) ⭐⭐ 가장 자랑할 부분

**"내 컴퓨터 안에서 직접 도는 AI"** 입니다. 답안이 인터넷으로 나가지 않아 **비공개·무료·무제한**입니다. 면접에서 가장 인상적인 기술 파트입니다.

### 9-1. `checkGPUSupport()` — 이 기기가 AI를 돌릴 수 있나?
- **위치:** `ai_grade.js:6`
- **하는 일:** 브라우저에 **WebGPU**(그래픽 카드를 웹에서 쓰는 기술)가 있는지 확인합니다(`navigator.gpu` `:8`, 실제 어댑터 요청 `:9`).
- **전체 역할:** AI 모델은 무거워서 GPU가 필요합니다. 지원 안 하면 `false`를 반환해 서버 채점으로 넘깁니다.
- **비유:** 무거운 짐을 들기 전에 "장비(지게차)가 있나?" 먼저 확인하는 것.

### 9-2. `getEngine()` — AI 모델을 딱 한 번만 불러오기 (싱글톤) ⭐
- **위치:** `ai_grade.js:63`
- **하는 일:**
  1. 이미 불러온 엔진이 있으면 **그대로 재사용**합니다(`:64`) — 이게 핵심.
  2. 없으면 로딩 화면을 띄우고(`:67`), 약 1.4GB짜리 Gemma 모델을 다운로드합니다(`CreateMLCEngine` `:71`). 진행률을 로딩바에 표시합니다(`:72`).
  3. 로딩 중 **취소**할 수 있게, "로딩 완료"와 "사용자 취소"를 경쟁시켜 먼저 끝나는 걸 받습니다(`Promise.race` `:74`).
  4. 성공하면 `engine`에 저장하고, "다운로드 완료" 표시를 localStorage에 남깁니다(`:82~83`).
- **싱글톤 패턴이란?** 만들기 비싼 자원(1.4GB 모델!)을 **딱 하나만 만들어 계속 재사용**하는 설계. 매번 새로 받으면 큰일 나니까요.
- **비유:** 무거운 백과사전을 한 번 책상에 꺼내두고 계속 참고하는 것. 볼 때마다 도서관에서 새로 빌려오지 않습니다.

### 9-3. `gradeWrittenOnDevice()` — 브라우저 안에서 채점 ⭐
- **위치:** `ai_grade.js:153`
- **하는 일:** 엔진을 얻고(`:155`, 없으면 `null` 반환→서버 폴백), 채점 프롬프트를 만들어(`buildPrompt` `:161`) 모델에 보냅니다(`:165`). **`temperature: 0`** 으로 설정한 게 핵심(`:167`).
- **temperature란?** AI의 "창의성/무작위성" 손잡이. 1이면 매번 다른 답, **0이면 거의 항상 같은 답**. 채점은 같은 답에 항상 같은 점수를 줘야 공정하므로 0으로 고정합니다(`:168~169` 주석).
- **전체 역할:** 이게 성공하면 `submitExam`이 그 결과를 서버로 보내 저장합니다. 실패하면 `null`을 반환해 서버 Gemini가 채점하게 넘깁니다(폴백).
- **비유:** temperature 0 = "규칙대로만 채점하는 엄격한 선생님". 기분 따라 점수가 안 바뀝니다.

### 9-4. `extractJson()` — AI 응답에서 JSON만 정밀 추출
- **위치:** `ai_grade.js:120`
- **하는 일:** 작은 온디바이스 모델(Gemma)은 종종 JSON 앞뒤에 잡말을 붙입니다. 그래서 첫 `[`부터 **괄호 짝을 세어가며** 정확히 짝 맞는 `]`까지만 잘라냅니다(`:126~136`). 문자열 안의 대괄호는 세지 않도록 따옴표 상태도 추적합니다(`:129~135`).
- **전체 역할:** 지저분한 AI 출력을 안전한 데이터로 바꾸는 파서. 작은 모델일수록 이런 방어가 중요합니다.
- **비유:** 편지에서 필요한 표(대괄호로 감싼 부분)만 정확히 오려내되, 표 안에 그려진 괄호에 헷갈리지 않는 것.

### 9-5. `_llmFriendlyError()` — 오류를 사람 말로 번역
- **위치:** `ai_grade.js:42`
- **하는 일:** 복잡한 기술 오류를 종류별로 알아보고("429 요청 과다", "GPU 문제" 등) **쉬운 안내 문구**로 바꿉니다(`:44~48`).
- **전체 역할:** 사용자가 겁먹지 않게, 그리고 "그동안 서버 Gemini로 채점된다"고 안심시킵니다.

### 9-6. `window.___ = ...` — 함수를 다른 파일에 공개하기
- **위치:** `ai_grade.js:146~151`, `:203`
- **하는 일:** `ai_grade.js` 안 함수는 기본적으로 그 파일에 **갇혀 있어** `app.js`가 못 봅니다. 그래서 `window.gradeWrittenOnDevice = ...` 처럼 **전역(window)에 올려** 다른 파일이 쓸 수 있게 공개합니다(`:146` 주석 설명).
- **스코프(scope)란?** 변수·함수가 "보이는 범위". 함수 안 변수는 함수 밖에서 안 보입니다. `window`는 모든 파일이 공유하는 "전체 공용 게시판"이라, 여기 올리면 어디서든 보입니다.
- **비유:** 내 방(파일) 안의 물건은 나만 쓰지만, 거실 공용 테이블(window)에 올려두면 온 가족이 씁니다.

### 9-7. `_reportDevice()` — 파일럿 데이터 수집
- **위치:** `ai_grade.js:52`
- **하는 일:** 이 기기가 WebGPU를 지원하는지, 모델 로딩에 성공했는지, 몇 초 걸렸는지를 서버에 보냅니다(`/api/pilot/device`).
- **전체 역할:** "온디바이스 AI가 실제 기기들에서 얼마나 잘 되나"를 데이터로 증명하기 위한 연구용 계측. (앞서 IRR '접근 격차' 실증) 관리자 콘솔(`app.py:795`)에서 통계로 볼 수 있습니다.

---

## 10. 랜딩(소개) 페이지 스크롤 애니메이션 — `static/script.js` (137줄) ⭐

VoQuiz 소개 사이트가 **스크롤에 따라 기기가 펴지고, 장면이 전환되는** 애플 스타일 애니메이션입니다. 부드럽고 배터리를 아끼는 기법이 들어 있습니다.

### 10-1. 수학 도구 3형제 — `clamp` / `lerp` / `range`
- **위치:** `script.js:9~12`
- **하는 일:**
  - `clamp`(`:9`): 값을 0~1 사이로 **가둡니다**(1.3이면 1로). 애니메이션 진행도가 범위를 벗어나지 않게.
  - `lerp`(`:10`): 두 값 사이를 부드럽게 **이어줍니다**(예: 작은 크기→큰 크기 중간값).
  - `range`(`:12`): 특정 스크롤 구간에서의 진행도(0~1)를 계산.
- **전체 역할:** 모든 스크롤 애니메이션 계산의 기본 재료. "스크롤이 이만큼 내려갔으니 기기 크기는 이만큼"을 계산합니다.
- **비유:** `clamp`는 "볼륨을 0~100으로 제한", `lerp`는 "두 지점 사이 매끄러운 슬라이드".

### 10-2. `IntersectionObserver` — 화면에 보이면 등장시키기 ⭐
- **위치:** `script.js:18` (등장), `:38` (숫자 카운트업)
- **하는 일:** 요소가 **화면에 들어오는 순간을 감지**해, `in` 클래스를 붙여 서서히 나타나게 합니다(`:20`). 한 번 나타나면 감시를 끕니다(`unobserve` `:21`)—다시 계산할 필요 없으니까.
- **숫자 카운트업(`:38`):** 통계 숫자(예: "0→1,240")가 화면에 보이면 1.4초 동안 **점점 커지며 세어 올라갑니다**. `easeOutCubic`(`:44`) 공식으로 처음엔 빠르고 끝엔 느리게, 자연스럽게 멈춥니다.
- **전체 역할:** 스크롤 위치를 매번 직접 계산하지 않고, 브라우저에게 "이게 보이면 알려줘"라고 맡기는 **효율적인** 방법.
- **비유:** 문에 센서를 달아 "사람이 들어오면 자동으로 불이 켜지는" 것. 내가 계속 지켜볼 필요가 없습니다.

### 10-3. `update()` + `requestAnimationFrame` — 스크롤 연동의 심장 ⭐⭐
- **위치:** 계산 `script.js:69`, 스크롤 처리 `onScroll` `:111`
- **하는 일:**
  1. 상단 진행바를 스크롤 비율만큼 채웁니다(`:74~75`).
  2. 내비게이션 바가 어두운 섹션 위에 오면 색을 반전시킵니다(`:79~84`).
  3. **PINNED(핀 고정) 구간**: 화면을 고정한 채 스크롤을 3등분해, 3개 장면을 하나씩 전환합니다(`:90~106`). 스크롤 위치로 "지금 몇 번째 장면인지(idx)"와 "그 장면 안 진행도(local)"를 계산해 진행 레일을 채웁니다.
- **성능 최적화(가장 중요):** 스크롤은 1초에 수백 번 발생합니다. 매번 계산하면 화면이 버벅입니다. 그래서 **`requestAnimationFrame`** 을 씁니다(`:114`). 이건 "브라우저가 화면을 다시 그리기 직전에 딱 한 번만 계산해줘"라는 뜻입니다. `ticking` 깃발(`:110~113`)로 중복 계산을 막습니다.
- **전체 역할:** 모든 스크롤 애니메이션(진행바·색반전·장면전환)을 **rAF 한 번에 모아** 처리해, 부드럽고 배터리를 아낍니다.
- **비유:** 사진사가 셔터를 아무 때나 누르는 게 아니라, **화면이 갱신되는 순간(초당 60번)에 딱 맞춰** 한 번만 찍는 것. 헛수고를 안 합니다.
- **`prefers-reduced-motion`(`:14`):** 사용자가 OS에서 "애니메이션 줄이기"를 켜놨으면 화려한 효과를 끕니다. **접근성 배려**입니다.

### 10-4. 반응형 분기 — `isMobile()`
- **위치:** 정의 `script.js:15`, 사용 `:35`, `:86`
- **하는 일:** 화면 폭이 960px 이하(모바일)면, 데스크톱의 무거운 "핀 고정" 방식 대신 **장면을 스크롤 시 하나씩 등장**시키는 가벼운 방식을 씁니다(`:35`).
- **전체 역할:** 큰 화면과 작은 화면에서 각각 최적의 방식으로 보여줍니다.

### 10-5. 소식 레일 좌우 버튼
- **위치:** `script.js:124`
- **하는 일:** 뉴스 카드들을 좌우 화살표로 부드럽게(`behavior: "smooth"`) 스크롤하고(`:127~128`), 끝에 닿으면 화살표를 비활성화합니다(`sync` `:129`).

---

## 11. 면접 예상 질문 & 30초 답변 모음

> 아래 질문이 나오면 위 항목의 줄번호를 짚으며 답하면 됩니다.

**Q. 비밀번호는 어떻게 안전하게 저장하나요?**
→ 평문 저장 안 합니다. 사람마다 다른 **소금(salt)** 을 섞어 **pbkdf2 해시를 20만 번** 돌려 저장합니다(`auth.py:67`). 해시는 되돌릴 수 없고, 소금 덕분에 같은 비밀번호도 다른 값이 돼 레인보우 테이블 공격을 막습니다. 로그인 대조는 타이밍 공격을 막는 상수시간 비교를 씁니다(`auth.py:81`).

**Q. 로그인 상태는 어떻게 유지되나요?**
→ 로그인 성공 시 임의 **세션 토큰**을 만들어(`auth.py:148`) DB에 저장하고 **httponly 쿠키**로 브라우저에 줍니다(`app.py:148`). 이후 요청마다 쿠키가 따라오고 서버가 주인을 확인합니다. 자바스크립트가 못 읽는 httponly라 토큰 탈취를 막습니다.

**Q. 무거운 AI 작업 중에 화면이 왜 안 멈추나요?**
→ 서버가 작업을 **백그라운드 스레드**에 맡기고 `job_id`만 즉시 돌려줍니다(`app.py:359`). 브라우저는 그 id로 **0.6초마다 진행 상황을 폴링**해 진행바를 그립니다(`app.js:1827`). 진동벨 구조입니다.

**Q. AI가 실패하거나 비용이 초과되면요?**
→ **3중 폴백**이 있습니다. ① 온디바이스 AI → ② 서버 Gemini(하루 한도) → ③ 규칙 기반 문자열 비교(`ai_quiz.py:358`). 어떤 상황에서도 서비스가 멈추지 않습니다.

**Q. 온디바이스 AI가 뭐가 특별한가요?**
→ 1.4GB 모델을 브라우저 안에 **딱 한 번 다운로드(싱글톤)** 해(`ai_grade.js:63`) 채점을 **기기 안에서** 합니다. 답안이 인터넷으로 안 나가 **비공개**, **무료·무제한**입니다. 채점은 공정성을 위해 **temperature 0**으로 항상 같은 결과를 냅니다(`ai_grade.js:167`).

**Q. 다크모드는 어떻게 구현했나요?**
→ 자바스크립트가 색을 하나씩 바꾸지 않고, 최상단 태그의 **`data-theme` 한 글자만** 바꿉니다(`app.js:1897`). CSS 변수들이 그걸 보고 화면 전체 색을 한꺼번에 교체합니다. 선택값은 **localStorage**에 저장해 다음 방문에도 유지되고, 첫 방문 땐 OS 설정을 따릅니다(`app.js:1891`).

**Q. 스크롤 애니메이션이 왜 안 버벅이나요?**
→ 스크롤은 초당 수백 번 발생하는데, **requestAnimationFrame**으로 화면 갱신 직전에 **딱 한 번만** 계산합니다(`script.js:111`). 등장 효과는 **IntersectionObserver**로 "보일 때만" 처리하고(`script.js:18`), 끝나면 감시를 끕니다. 배터리와 성능을 아끼는 설계입니다.

**Q. 데이터가 회원끼리 안 섞이나요?**
→ 모든 표에 **user_id 외래키**가 있고(`database.py`), 거의 모든 API가 `Depends(current_user)`로 **본인 소유만** 접근하게 검문합니다(`app.py:84`). SQL은 `?` 자리표시자로 **인젝션을 막습니다**.

---

## 12. 파일 지도 (한눈에)

```
VoQuiz/
├── app.py            ← ② 서버 심장. 모든 /api 창구 (920줄)
├── auth.py           ← 로그인·비밀번호 해시·세션·이메일 인증
├── database.py       ← ③ SQLite 표 정의 + 접근 도구
├── progress.py       ← 진행바용 작업 상태 저장(메모리)
├── ai_extract.py     ← PDF/사진 → 단어 추출 (OCR+Gemini, 페이지 병렬)
├── ai_quiz.py        ← 문제 생성 + 채점 (핵심, 3중 폴백)
├── pdf_export.py     ← 시험지·리포트 PDF 생성 (한글 폰트)
├── data/vocashot.db  ← 실제 데이터가 담긴 파일
└── static/
    ├── script.js         ← ① 랜딩 스크롤 애니메이션(rAF·Observer)
    └── app/
        ├── app.js        ← ① 앱 두뇌: api·폴링·타이머·다크모드 (2465줄)
        └── ai_grade.js   ← ① 온디바이스 AI (WebGPU·싱글톤·채점)
```

**한 문장 요약:** 브라우저(`app.js`/`ai_grade.js`)가 서버(`app.py`)에 요청하면, 서버가 AI(`ai_quiz.py`/`ai_extract.py`)와 창고(`database.py`)를 부려 일을 처리하고, 오래 걸리는 일은 진동벨(`progress.py`+`pollProgress`)로 진행 상황을 보여주며, 화면은 다크모드와 부드러운 스크롤 애니메이션(`script.js`)으로 꾸며집니다.
